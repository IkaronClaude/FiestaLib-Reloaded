// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 9</summary>
public class PROTO_NC_USP_USER_CHARACTER_DELETE
{
    public uint nUserNo;
    public byte nWorldNo;
    public uint nCharNo;

    public void Read(BinaryReader reader)
    {
        nUserNo = reader.ReadUInt32();
        nWorldNo = reader.ReadByte();
        nCharNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nUserNo);
        writer.Write(nWorldNo);
        writer.Write(nCharNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 50</summary>
public class PROTO_NC_USP_USER_CHARACTER_INSERT
{
    public uint nUserNo;
    public byte nWorldNo;
    public uint nCharNo;
    public sbyte[] sCharName = new sbyte[40];
    public byte nClass;

    public void Read(BinaryReader reader)
    {
        nUserNo = reader.ReadUInt32();
        nWorldNo = reader.ReadByte();
        nCharNo = reader.ReadUInt32();
        for (int i = 0; i < 40; i++)
            sCharName[i] = reader.ReadSByte();
        nClass = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nUserNo);
        writer.Write(nWorldNo);
        writer.Write(nCharNo);
        for (int i = 0; i < 40; i++)
            writer.Write(sCharName[i]);
        writer.Write(nClass);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

