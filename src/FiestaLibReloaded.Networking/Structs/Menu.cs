// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 12</summary>
public class PROTO_NC_MENU_OPENSTORAGE_CMD
{
    public ulong cen;
    public byte maxpage;
    public byte curpage;
    public byte nOpenType;
    public byte itemcounter;
    public PROTO_ITEMPACKET_INFORM[] itemarray = [];

    public void Read(BinaryReader reader)
    {
        cen = reader.ReadUInt64();
        maxpage = reader.ReadByte();
        curpage = reader.ReadByte();
        nOpenType = reader.ReadByte();
        itemcounter = reader.ReadByte();
        itemarray = new PROTO_ITEMPACKET_INFORM[itemcounter];
        for (int i = 0; i < itemcounter; i++)
        {
            itemarray[i] = new PROTO_ITEMPACKET_INFORM();
            itemarray[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(cen);
        writer.Write(maxpage);
        writer.Write(curpage);
        writer.Write(nOpenType);
        writer.Write(itemcounter);
        for (int i = 0; i < itemarray.Length; i++)
            itemarray[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MENU_SERVERMENU_ACK
{
    public byte reply;

    public void Read(BinaryReader reader)
    {
        reply = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(reply);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 142</summary>
public class PROTO_NC_MENU_SERVERMENU_REQ
{
    public byte[] title = new byte[128];
    public byte priority;
    public ushort npcHandle;
    public SHINE_XY_TYPE npcPosition = new();
    public ushort limitRange;
    public byte menunum;
    public SERVERMENU[] menu = [];

    public void Read(BinaryReader reader)
    {
        title = reader.ReadBytes(128);
        priority = reader.ReadByte();
        npcHandle = reader.ReadUInt16();
        npcPosition.Read(reader);
        limitRange = reader.ReadUInt16();
        menunum = reader.ReadByte();
        menu = new SERVERMENU[menunum];
        for (int i = 0; i < menunum; i++)
        {
            menu[i] = new SERVERMENU();
            menu[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(title, 0, 128);
        writer.Write(priority);
        writer.Write(npcHandle);
        npcPosition.Write(writer);
        writer.Write(limitRange);
        writer.Write(menunum);
        for (int i = 0; i < menu.Length; i++)
            menu[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MENU_SHOPOPEN_CMD
{
    public ushort itemnum;
    public ushort npc;
    // PROTO_NC_MENU_SHOPOPEN_CMD::MENUITEM[0] itemlist at offset 4 (unsupported type)

    public void Read(BinaryReader reader)
    {
        itemnum = reader.ReadUInt16();
        npc = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(itemnum);
        writer.Write(npc);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 24</summary>
public class PROTO_NC_MENU_SHOPOPENSOULSTONE_CMD
{
    // PROTO_NC_MENU_SHOPOPENSOULSTONE_CMD::SOULSTONEMENU hp at offset 0 (unsupported type)
    // PROTO_NC_MENU_SHOPOPENSOULSTONE_CMD::SOULSTONEMENU sp at offset 12 (unsupported type)

    public void Read(BinaryReader reader)
    {
        reader.ReadBytes(12); // skip PROTO_NC_MENU_SHOPOPENSOULSTONE_CMD::SOULSTONEMENU hp
        reader.ReadBytes(12); // skip PROTO_NC_MENU_SHOPOPENSOULSTONE_CMD::SOULSTONEMENU sp
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(new byte[12]); // skip PROTO_NC_MENU_SHOPOPENSOULSTONE_CMD::SOULSTONEMENU hp
        writer.Write(new byte[12]); // skip PROTO_NC_MENU_SHOPOPENSOULSTONE_CMD::SOULSTONEMENU sp
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MENU_SHOPOPENTABLE_CMD
{
    public ushort itemnum;
    public ushort npc;
    // PROTO_NC_MENU_SHOPOPENTABLE_CMD::MENUITEM[0] itemlist at offset 4 (unsupported type)

    public void Read(BinaryReader reader)
    {
        itemnum = reader.ReadUInt16();
        npc = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(itemnum);
        writer.Write(npc);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

