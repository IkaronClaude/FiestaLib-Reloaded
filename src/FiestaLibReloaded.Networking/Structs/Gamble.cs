// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 8</summary>
public class PROTO_NC_GAMBLE_COIN_BUY_REQ
{
    public ulong nCoin;

    public void Read(BinaryReader reader)
    {
        nCoin = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nCoin);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 8</summary>
public class PROTO_NC_GAMBLE_COIN_CHANGE_CMD
{
    public ulong nCoin;

    public void Read(BinaryReader reader)
    {
        nCoin = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nCoin);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_GAMBLE_COIN_DB_BUY_ACK
{
    public NETPACKETZONEHEADER nHeader = new();
    public ushort nLockIndex;
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nHeader.Read(reader);
        nLockIndex = reader.ReadUInt16();
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        nHeader.Write(writer);
        writer.Write(nLockIndex);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 32</summary>
public class PROTO_NC_GAMBLE_COIN_DB_BUY_REQ
{
    public NETPACKETZONEHEADER nHeader = new();
    public ushort nLockIndex;
    public ulong nCoin;
    public ulong nMoney;
    public uint nCanExchangeMaxCoin;
    public int tExchangedCoinDate;

    public void Read(BinaryReader reader)
    {
        nHeader.Read(reader);
        nLockIndex = reader.ReadUInt16();
        nCoin = reader.ReadUInt64();
        nMoney = reader.ReadUInt64();
        nCanExchangeMaxCoin = reader.ReadUInt32();
        tExchangedCoinDate = reader.ReadInt32();
    }

    public void Write(BinaryWriter writer)
    {
        nHeader.Write(writer);
        writer.Write(nLockIndex);
        writer.Write(nCoin);
        writer.Write(nMoney);
        writer.Write(nCanExchangeMaxCoin);
        writer.Write(tExchangedCoinDate);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_GAMBLE_COIN_DB_SELL_ACK
{
    public NETPACKETZONEHEADER nHeader = new();
    public ushort nLockIndex;
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nHeader.Read(reader);
        nLockIndex = reader.ReadUInt16();
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        nHeader.Write(writer);
        writer.Write(nLockIndex);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 24</summary>
public class PROTO_NC_GAMBLE_COIN_DB_SELL_REQ
{
    public NETPACKETZONEHEADER nHeader = new();
    public ushort nLockIndex;
    public ulong nCoin;
    public ulong nMoney;

    public void Read(BinaryReader reader)
    {
        nHeader.Read(reader);
        nLockIndex = reader.ReadUInt16();
        nCoin = reader.ReadUInt64();
        nMoney = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        nHeader.Write(writer);
        writer.Write(nLockIndex);
        writer.Write(nCoin);
        writer.Write(nMoney);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 18</summary>
public class PROTO_NC_GAMBLE_COIN_DB_USE_COINITEM_ACK
{
    public NETPACKETZONEHEADER nHeader = new();
    public ulong nCoin;
    public ushort nLockIndex;
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nHeader.Read(reader);
        nCoin = reader.ReadUInt64();
        nLockIndex = reader.ReadUInt16();
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        nHeader.Write(writer);
        writer.Write(nCoin);
        writer.Write(nLockIndex);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 24</summary>
public class PROTO_NC_GAMBLE_COIN_DB_USE_COINITEM_REQ
{
    public NETPACKETZONEHEADER nHeader = new();
    public SHINE_ITEM_REGISTNUMBER nItemReg = new();
    public ulong nCoin;
    public ushort nLockIndex;

    public void Read(BinaryReader reader)
    {
        nHeader.Read(reader);
        nItemReg.Read(reader);
        nCoin = reader.ReadUInt64();
        nLockIndex = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        nHeader.Write(writer);
        nItemReg.Write(writer);
        writer.Write(nCoin);
        writer.Write(nLockIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 8</summary>
public class PROTO_NC_GAMBLE_COIN_USE_COINITEM_MESSAGE_CMD
{
    public ulong nAddCoin;

    public void Read(BinaryReader reader)
    {
        nAddCoin = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nAddCoin);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_GAMBLE_COIN_VIPCARD_UI_OPEN_REQ
{
    public byte nInvenSlot;

    public void Read(BinaryReader reader)
    {
        nInvenSlot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nInvenSlot);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 3</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_ALL_RANK_ACK
{
    public ushort nError;
    public byte nRankAllCnt;
    public GDT_DiceRankInfo[] RankAll = [];

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nRankAllCnt = reader.ReadByte();
        RankAll = new GDT_DiceRankInfo[nRankAllCnt];
        for (int i = 0; i < nRankAllCnt; i++)
        {
            RankAll[i] = new GDT_DiceRankInfo();
            RankAll[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nRankAllCnt);
        for (int i = 0; i < RankAll.Length; i++)
            RankAll[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_ALL_RANK_REQ
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 7</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_BET_START_CMD
{
    // DICE_TAISAI_GAME_MODE bModeType at offset 0 (unsupported type)
    public DiceTaiSaiInfo CurrentRollingDice = new();

    public void Read(BinaryReader reader)
    {
        CurrentRollingDice.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        CurrentRollingDice.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_BETTING_ACK
{
    public ushort nError;
    public ushort nBettingIndex;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nBettingIndex = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nBettingIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_BETTING_CANCEL_ACK
{
    public ushort nError;
    public ushort nBettingCancelIndex;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nBettingCancelIndex = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nBettingCancelIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_BETTING_CANCEL_CMD
{
    public ushort nPlayerHnd;
    public DiceTaiSaiBetting BettingCancelInfo = new();

    public void Read(BinaryReader reader)
    {
        nPlayerHnd = reader.ReadUInt16();
        BettingCancelInfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPlayerHnd);
        BettingCancelInfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_BETTING_CANCEL_REQ
{
    public DiceTaiSaiBetting BettingCancelInfo = new();
    public ushort nBettingCancelIndex;

    public void Read(BinaryReader reader)
    {
        BettingCancelInfo.Read(reader);
        nBettingCancelIndex = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        BettingCancelInfo.Write(writer);
        writer.Write(nBettingCancelIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_BETTING_CMD
{
    public ushort nPlayerHnd;
    public DiceTaiSaiBetting BettingInfo = new();

    public void Read(BinaryReader reader)
    {
        nPlayerHnd = reader.ReadUInt16();
        BettingInfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPlayerHnd);
        BettingInfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_BETTING_REQ
{
    public DiceTaiSaiBetting BettingInfo = new();
    public ushort nBettingIndex;

    public void Read(BinaryReader reader)
    {
        BettingInfo.Read(reader);
        nBettingIndex = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        BettingInfo.Write(writer);
        writer.Write(nBettingIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 3</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_CURR_RANK_ACK
{
    public ushort nError;
    public byte nRankCurrCnt;
    public GDT_DiceRankInfo[] RankCurr = [];

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nRankCurrCnt = reader.ReadByte();
        RankCurr = new GDT_DiceRankInfo[nRankCurrCnt];
        for (int i = 0; i < nRankCurrCnt; i++)
        {
            RankCurr[i] = new GDT_DiceRankInfo();
            RankCurr[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nRankCurrCnt);
        for (int i = 0; i < RankCurr.Length; i++)
            RankCurr[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_CURR_RANK_REQ
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 9</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_DB_ALL_RANK_ACK
{
    public ushort nError;
    public NETPACKETZONEHEADER header = new();
    public byte nRankAllCnt;
    public GDT_DiceRankInfo[] RankAll = [];

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        header.Read(reader);
        nRankAllCnt = reader.ReadByte();
        RankAll = new GDT_DiceRankInfo[nRankAllCnt];
        for (int i = 0; i < nRankAllCnt; i++)
        {
            RankAll[i] = new GDT_DiceRankInfo();
            RankAll[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        header.Write(writer);
        writer.Write(nRankAllCnt);
        for (int i = 0; i < RankAll.Length; i++)
            RankAll[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_DB_ALL_RANK_REQ
{
    public NETPACKETZONEHEADER header = new();

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 14</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_DB_DICE_ROLL_RESULT_ACK
{
    public ushort nError;
    public int nGameNo;
    public NETPACKETZONEHEADER header = new();
    public ushort nResultCnt;
    public GDT_DiceRollApplyResult[] DiceRollResult = [];

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nGameNo = reader.ReadInt32();
        header.Read(reader);
        nResultCnt = reader.ReadUInt16();
        DiceRollResult = new GDT_DiceRollApplyResult[nResultCnt];
        for (int i = 0; i < nResultCnt; i++)
        {
            DiceRollResult[i] = new GDT_DiceRollApplyResult();
            DiceRollResult[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nGameNo);
        header.Write(writer);
        writer.Write(nResultCnt);
        for (int i = 0; i < DiceRollResult.Length; i++)
            DiceRollResult[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 12</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_DB_DICE_ROLL_RESULT_LOG_CMD
{
    public NETPACKETZONEHEADER header = new();
    public int nGameNo;
    public ushort nResultLogCnt;
    public GDT_DiceRollApplyResultLog[] DiceRollResultLog = [];

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        nGameNo = reader.ReadInt32();
        nResultLogCnt = reader.ReadUInt16();
        DiceRollResultLog = new GDT_DiceRollApplyResultLog[nResultLogCnt];
        for (int i = 0; i < nResultLogCnt; i++)
        {
            DiceRollResultLog[i] = new GDT_DiceRollApplyResultLog();
            DiceRollResultLog[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(nGameNo);
        writer.Write(nResultLogCnt);
        for (int i = 0; i < DiceRollResultLog.Length; i++)
            DiceRollResultLog[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 11</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_DB_DICE_ROLL_RESULT_REQ
{
    public NETPACKETZONEHEADER header = new();
    public DiceTaiSaiInfo DiceNum = new();
    public ushort nResultCnt;
    public GDT_DiceRollApplyResult[] DiceRollResult = [];

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        DiceNum.Read(reader);
        nResultCnt = reader.ReadUInt16();
        DiceRollResult = new GDT_DiceRollApplyResult[nResultCnt];
        for (int i = 0; i < nResultCnt; i++)
        {
            DiceRollResult[i] = new GDT_DiceRollApplyResult();
            DiceRollResult[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        DiceNum.Write(writer);
        writer.Write(nResultCnt);
        for (int i = 0; i < DiceRollResult.Length; i++)
            DiceRollResult[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_DICE_ROLL_CMD
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 5</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_DICE_ROLL_RESULT_CMD
{
    public ushort nError;
    public DiceTaiSaiInfo nDiceInfo = new();

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nDiceInfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        nDiceInfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_DICE_ROLL_RESULT_EMOTION_CMD
{
    public byte nGamerCnt;
    public GDT_EmotionInfo[] Emotion = [];

    public void Read(BinaryReader reader)
    {
        nGamerCnt = reader.ReadByte();
        Emotion = new GDT_EmotionInfo[nGamerCnt];
        for (int i = 0; i < nGamerCnt; i++)
        {
            Emotion[i] = new GDT_EmotionInfo();
            Emotion[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nGamerCnt);
        for (int i = 0; i < Emotion.Length; i++)
            Emotion[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 3</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_GAME_BETTING_INFO_CMD
{
    public byte PartMask;
    public ushort nBettingCnt;
    public GDT_BettingInfo[] Betting = [];

    public void Read(BinaryReader reader)
    {
        PartMask = reader.ReadByte();
        nBettingCnt = reader.ReadUInt16();
        Betting = new GDT_BettingInfo[nBettingCnt];
        for (int i = 0; i < nBettingCnt; i++)
        {
            Betting[i] = new GDT_BettingInfo();
            Betting[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(PartMask);
        writer.Write(nBettingCnt);
        for (int i = 0; i < Betting.Length; i++)
            Betting[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 50</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_GAME_JOIN_ACK
{
    public ushort nError;
    public DiceTaiSaiInfo[] DiceHistory = new DiceTaiSaiInfo[7];
    // DICE_TAISAI_GAME_STATUS nGameStatus at offset 23 (unsupported type)
    // DICE_TAISAI_GAME_MODE bModeType at offset 27 (unsupported type)
    public ushort nTimer;
    public uint nInterval;
    public ushort nGetSysRate;
    public ulong nMaxBetMoney;
    public ushort nUseMinLv;
    public byte nGamerCnt;
    public ushort[] nGamerHnd = [];

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        for (int i = 0; i < 7; i++)
        {
            DiceHistory[i] = new DiceTaiSaiInfo();
            DiceHistory[i].Read(reader);
        }
        nTimer = reader.ReadUInt16();
        nInterval = reader.ReadUInt32();
        nGetSysRate = reader.ReadUInt16();
        nMaxBetMoney = reader.ReadUInt64();
        nUseMinLv = reader.ReadUInt16();
        nGamerCnt = reader.ReadByte();
        nGamerHnd = new ushort[nGamerCnt];
        for (int i = 0; i < nGamerCnt; i++)
            nGamerHnd[i] = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        for (int i = 0; i < 7; i++)
            DiceHistory[i].Write(writer);
        writer.Write(nTimer);
        writer.Write(nInterval);
        writer.Write(nGetSysRate);
        writer.Write(nMaxBetMoney);
        writer.Write(nUseMinLv);
        writer.Write(nGamerCnt);
        for (int i = 0; i < nGamerHnd.Length; i++)
            writer.Write(nGamerHnd[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_GAME_JOIN_CMD
{
    public ushort nPlayerHnd;

    public void Read(BinaryReader reader)
    {
        nPlayerHnd = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPlayerHnd);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_GAME_JOIN_REQ
{
    public ushort nNpcHandle;
    public byte nWhereStand;
    public byte nDirect;

    public void Read(BinaryReader reader)
    {
        nNpcHandle = reader.ReadUInt16();
        nWhereStand = reader.ReadByte();
        nDirect = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nNpcHandle);
        writer.Write(nWhereStand);
        writer.Write(nDirect);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_GAME_LEAVE_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 3</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_GAME_LEAVE_CMD
{
    public byte bReserve;
    public ushort nPlayerHnd;

    public void Read(BinaryReader reader)
    {
        bReserve = reader.ReadByte();
        nPlayerHnd = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bReserve);
        writer.Write(nPlayerHnd);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_LARGE_AMOUNT_CMD
{
    public byte nGamerCnt;
    public GDT_LargeAmountInfo[] LargeAmount = [];

    public void Read(BinaryReader reader)
    {
        nGamerCnt = reader.ReadByte();
        LargeAmount = new GDT_LargeAmountInfo[nGamerCnt];
        for (int i = 0; i < nGamerCnt; i++)
        {
            LargeAmount[i] = new GDT_LargeAmountInfo();
            LargeAmount[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nGamerCnt);
        for (int i = 0; i < LargeAmount.Length; i++)
            LargeAmount[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 7</summary>
public class PROTO_NC_GAMBLE_DICE_TAISAI_TIMER_CMD
{
    public byte bIsStart;
    public ushort nTimer;
    public uint nInterval;

    public void Read(BinaryReader reader)
    {
        bIsStart = reader.ReadByte();
        nTimer = reader.ReadUInt16();
        nInterval = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bIsStart);
        writer.Write(nTimer);
        writer.Write(nInterval);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 3</summary>
public class PROTO_NC_GAMBLE_ENTER_PLAYER_DIRECT_CMD
{
    public ushort nHandle;
    public byte nDirect;

    public void Read(BinaryReader reader)
    {
        nHandle = reader.ReadUInt16();
        nDirect = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nHandle);
        writer.Write(nDirect);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 8</summary>
public class PROTO_NC_GAMBLE_EXCHANGECOIN_CHANGE_CMD
{
    public ulong nExchangeCoin;

    public void Read(BinaryReader reader)
    {
        nExchangeCoin = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nExchangeCoin);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 12</summary>
public class PROTO_NC_GAMBLE_EXCHANGEDCOIN_DB_INFO_ACK
{
    public uint nCharNo;
    public ulong nExchangedCoin;

    public void Read(BinaryReader reader)
    {
        nCharNo = reader.ReadUInt32();
        nExchangedCoin = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nCharNo);
        writer.Write(nExchangedCoin);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 8</summary>
public class PROTO_NC_GAMBLE_EXCHANGEDCOIN_DB_INFO_REQ
{
    public uint nCharNo;
    public int dChackDate;

    public void Read(BinaryReader reader)
    {
        nCharNo = reader.ReadUInt32();
        dChackDate = reader.ReadInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nCharNo);
        writer.Write(dChackDate);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_GAMBLE_EXCHANGEDCOIN_INIT_CMD
{
    public int dChackDate;

    public void Read(BinaryReader reader)
    {
        dChackDate = reader.ReadInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(dChackDate);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_GAMBLE_PLAYERACT_CMD
{
    public ushort playerhandle;
    public uint effect_body;

    public void Read(BinaryReader reader)
    {
        playerhandle = reader.ReadUInt16();
        effect_body = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(playerhandle);
        writer.Write(effect_body);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 8</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_DB_GAMEINFO_ACK
{
    public byte Machine;
    public ushort nError;
    public uint JackPotPrice;
    public byte RankNumber;
    public SLOTMACHINE_JackPotRank[] RankList = [];

    public void Read(BinaryReader reader)
    {
        Machine = reader.ReadByte();
        nError = reader.ReadUInt16();
        JackPotPrice = reader.ReadUInt32();
        RankNumber = reader.ReadByte();
        RankList = new SLOTMACHINE_JackPotRank[RankNumber];
        for (int i = 0; i < RankNumber; i++)
        {
            RankList[i] = new SLOTMACHINE_JackPotRank();
            RankList[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(Machine);
        writer.Write(nError);
        writer.Write(JackPotPrice);
        writer.Write(RankNumber);
        for (int i = 0; i < RankList.Length; i++)
            RankList[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_DB_GAMEINFO_REQ
{
    public byte nSlotMachineNum;
    // PROTO_NC_GAMBLE_SLOTMACHINE_DB_GAMEINFO_REQ::<unnamed-type-MachineIndex>[0] MachineIndex at offset 1 (unsupported type)

    public void Read(BinaryReader reader)
    {
        nSlotMachineNum = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nSlotMachineNum);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_DB_RESULT_ACK
{
    public NETPACKETZONEHEADER header = new();
    public ushort lockindex;
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        lockindex = reader.ReadUInt16();
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(lockindex);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 36</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_DB_RESULT_REQ
{
    public NETPACKETZONEHEADER header = new();
    public ushort lockindex;
    public uint PlayerRegistNumber;
    public sbyte[] CardDisplay = new sbyte[10];
    public byte IsJackPot;
    public uint JackPotAccumulate;
    public ushort BetUnit;
    public byte BetLine;
    public ushort nJackpotRate;
    public uint WinMoney;

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        lockindex = reader.ReadUInt16();
        PlayerRegistNumber = reader.ReadUInt32();
        for (int i = 0; i < 10; i++)
            CardDisplay[i] = reader.ReadSByte();
        IsJackPot = reader.ReadByte();
        JackPotAccumulate = reader.ReadUInt32();
        BetUnit = reader.ReadUInt16();
        BetLine = reader.ReadByte();
        nJackpotRate = reader.ReadUInt16();
        WinMoney = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(lockindex);
        writer.Write(PlayerRegistNumber);
        for (int i = 0; i < 10; i++)
            writer.Write(CardDisplay[i]);
        writer.Write(IsJackPot);
        writer.Write(JackPotAccumulate);
        writer.Write(BetUnit);
        writer.Write(BetLine);
        writer.Write(nJackpotRate);
        writer.Write(WinMoney);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_GAME_JOIN_ACK
{
    public ushort BettingUnit;
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        BettingUnit = reader.ReadUInt16();
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(BettingUnit);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_GAME_JOIN_REQ
{
    public ushort nNpcHandle;
    public byte nWhereStand;
    public byte nDirect;

    public void Read(BinaryReader reader)
    {
        nNpcHandle = reader.ReadUInt16();
        nWhereStand = reader.ReadByte();
        nDirect = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nNpcHandle);
        writer.Write(nWhereStand);
        writer.Write(nDirect);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_GAME_LEAVE_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_JACKPOTINFO_CMD
{
    public byte JackPotNum;
    // PROTO_NC_GAMBLE_SLOTMACHINE_JACKPOTINFO_CMD::<unnamed-type-JackPotList>[0] JackPotList at offset 1 (unsupported type)

    public void Read(BinaryReader reader)
    {
        JackPotNum = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(JackPotNum);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_JACKPOTRANKING_ACK
{
    public byte ListLen;
    public SLOTMACHINE_JackPotRank[] RankList = [];

    public void Read(BinaryReader reader)
    {
        ListLen = reader.ReadByte();
        RankList = new SLOTMACHINE_JackPotRank[ListLen];
        for (int i = 0; i < ListLen; i++)
        {
            RankList[i] = new SLOTMACHINE_JackPotRank();
            RankList[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(ListLen);
        for (int i = 0; i < RankList.Length; i++)
            RankList[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 28</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_SOMEONE_GET_JACKPOT_CMD
{
    public Name5 charid = new();
    public ulong price;

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        price = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        writer.Write(price);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_START_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_START_REQ
{
    public SLOTMACHINE_BettingLine betting = new();

    public void Read(BinaryReader reader)
    {
        betting.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        betting.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 11</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_STOPBUTTON_ACK
{
    public ushort nError;
    public SLOTMACHINE_ScreenState screen = new();

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        screen.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        screen.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_WHEELSTOP_ACK
{
    public ushort nError;
    public ushort bet;
    public uint win;
    public SLOTMACHINE_WinState WinLine = new();

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        bet = reader.ReadUInt16();
        win = reader.ReadUInt32();
        WinLine.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(bet);
        writer.Write(win);
        WinLine.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_GAMBLE_SLOTMACHINE_WINRANKING_ACK
{
    public byte ListLen;
    public SLOTMACHINE_JackPotRank[] RankList = [];

    public void Read(BinaryReader reader)
    {
        ListLen = reader.ReadByte();
        RankList = new SLOTMACHINE_JackPotRank[ListLen];
        for (int i = 0; i < ListLen; i++)
        {
            RankList[i] = new SLOTMACHINE_JackPotRank();
            RankList[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(ListLen);
        for (int i = 0; i < RankList.Length; i++)
            RankList[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 9</summary>
public class PROTO_NC_GAMBLE_TYPE_AND_WHERE_STAND_ACK
{
    public ushort nError;
    public ushort nNpcHandle;
    // GAMBLE_TYPE nGambleType at offset 4 (unsupported type)
    public byte nWhereStand;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nNpcHandle = reader.ReadUInt16();
        nWhereStand = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nNpcHandle);
        writer.Write(nWhereStand);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_GAMBLE_TYPE_AND_WHERE_STAND_REQ
{
    public ushort nNpcHandle;

    public void Read(BinaryReader reader)
    {
        nNpcHandle = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nNpcHandle);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 18</summary>
public class PROTO_NC_GAMBLE_WORLD_PREVMAPNAME_CMD
{
    public ushort nWldHandle;
    public Name3 sPrevMapName = new();
    public uint nCharNo;

    public void Read(BinaryReader reader)
    {
        nWldHandle = reader.ReadUInt16();
        sPrevMapName.Read(reader);
        nCharNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nWldHandle);
        sPrevMapName.Write(writer);
        writer.Write(nCharNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 12</summary>
public class PROTO_NC_GAMBLE_ZONE_PREVMAPNAME_CMD
{
    public Name3 sPrevMapName = new();

    public void Read(BinaryReader reader)
    {
        sPrevMapName.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sPrevMapName.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

