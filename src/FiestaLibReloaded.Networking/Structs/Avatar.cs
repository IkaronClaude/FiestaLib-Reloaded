// <auto-generated />
using FiestaLibReloaded.Networking;
using FiestaLibReloaded.Networking.Enums;

namespace FiestaLibReloaded.Networking.Structs;

/// <summary>
/// Department: Avatar (0x05) | Opcode: 1 | Full: 0x1401
/// sizeof = 25
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 1)]
public class PROTO_NC_AVATAR_CREATE_REQ : IFiestaPacketBody
{
    public byte slotnum;
    public Name5 name = new();
    public PROTO_AVATAR_SHAPE_INFO char_shape = new();

    public void Read(BinaryReader reader)
    {
        slotnum = reader.ReadByte();
        name.Read(reader);
        char_shape.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(slotnum);
        name.Write(writer);
        char_shape.Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 2 | Full: 0x1402
/// sizeof = 33
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 2)]
public class PROTO_NC_AVATAR_CREATEDATA_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public uint usrregnum;
    public byte createworld;
    public byte adminlevel;
    public PROTO_NC_AVATAR_CREATE_REQ clientdata = new();

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        usrregnum = reader.ReadUInt32();
        createworld = reader.ReadByte();
        adminlevel = reader.ReadByte();
        clientdata.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(usrregnum);
        writer.Write(createworld);
        writer.Write(adminlevel);
        clientdata.Write(writer);
    }
}

/// <summary>sizeof = 36</summary>
public class PROTO_NC_AVATAR_CREATEDATA_REQ_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_CREATEDATA_REQ req = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        req.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        req.Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 3 | Full: 0x1403
/// sizeof = 22
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 3)]
public class PROTO_NC_AVATAR_CREATEDATAFAIL_ACK : IFiestaPacketBody
{
    public Name5 charid = new();
    public ushort err;

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        writer.Write(err);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 5 | Full: 0x1405
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 5)]
public class PROTO_NC_AVATAR_CREATEDATASUC_ACK : IFiestaPacketBody
{
    public byte numofavatar;
    public PROTO_AVATARINFORMATION[] avatar = [];

    public void Read(BinaryReader reader)
    {
        numofavatar = reader.ReadByte();
        avatar = new PROTO_AVATARINFORMATION[numofavatar];
        for (int i = 0; i < numofavatar; i++)
        {
            avatar[i] = new PROTO_AVATARINFORMATION();
            avatar[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(numofavatar);
        for (int i = 0; i < avatar.Length; i++)
            avatar[i].Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 4 | Full: 0x1404
/// sizeof = 2
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 4)]
public class PROTO_NC_AVATAR_CREATEFAIL_ACK : IFiestaPacketBody
{
    public ushort err;

    public void Read(BinaryReader reader)
    {
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(err);
    }
}

/// <summary>sizeof = 5</summary>
public class PROTO_NC_AVATAR_CREATEFAIL_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_CREATEFAIL_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 6 | Full: 0x1406
/// sizeof = 131
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 6)]
public class PROTO_NC_AVATAR_CREATESUCC_ACK : IFiestaPacketBody
{
    public byte numofavatar;
    public PROTO_AVATARINFORMATION avatar = new();

    public void Read(BinaryReader reader)
    {
        numofavatar = reader.ReadByte();
        avatar.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(numofavatar);
        avatar.Write(writer);
    }
}

/// <summary>sizeof = 134</summary>
public class PROTO_NC_AVATAR_CREATESUCC_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_CREATESUCC_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 7 | Full: 0x1407
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 7)]
public class PROTO_NC_AVATAR_ERASE_REQ : IFiestaPacketBody
{
    public byte slot;

    public void Read(BinaryReader reader)
    {
        slot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(slot);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 8 | Full: 0x1408
/// sizeof = 15
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 8)]
public class PROTO_NC_AVATAR_ERASEDATA_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public uint usrregnum;
    public uint avatar;
    public byte slot;
    public uint nNo;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        usrregnum = reader.ReadUInt32();
        avatar = reader.ReadUInt32();
        slot = reader.ReadByte();
        nNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(usrregnum);
        writer.Write(avatar);
        writer.Write(slot);
        writer.Write(nNo);
    }
}

/// <summary>sizeof = 18</summary>
public class PROTO_NC_AVATAR_ERASEDATA_REQ_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_ERASEDATA_REQ req = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        req.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        req.Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 9 | Full: 0x1409
/// sizeof = 4
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 9)]
public class PROTO_NC_AVATAR_ERASEDATAFAIL_ACK : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public ushort err;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(err);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 11 | Full: 0x140B
/// sizeof = 15
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 11)]
public class PROTO_NC_AVATAR_ERASEDATASUC_ACK : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public uint usrregnum;
    public uint avatar;
    public byte slot;
    public uint nNo;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        usrregnum = reader.ReadUInt32();
        avatar = reader.ReadUInt32();
        slot = reader.ReadByte();
        nNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(usrregnum);
        writer.Write(avatar);
        writer.Write(slot);
        writer.Write(nNo);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 10 | Full: 0x140A
/// sizeof = 2
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 10)]
public class PROTO_NC_AVATAR_ERASEFAIL_ACK : IFiestaPacketBody
{
    public ushort err;

    public void Read(BinaryReader reader)
    {
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(err);
    }
}

/// <summary>sizeof = 5</summary>
public class PROTO_NC_AVATAR_ERASEFAIL_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_ERASEFAIL_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_AVATAR_ERASESUC_ACK : IFiestaPacketBody
{
    public byte slot;

    public void Read(BinaryReader reader)
    {
        slot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(slot);
    }
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_AVATAR_ERASESUC_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_ERASESUC_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 20 | Full: 0x1414
/// sizeof = 9
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 20)]
public class PROTO_NC_AVATAR_GUILD_DATA_ACK : IFiestaPacketBody
{
    public uint nGuildNo;
    public uint chrregnum;
    public byte slot;
    public GUILD_CLIENT[] Guild = [];

    public void Read(BinaryReader reader)
    {
        nGuildNo = reader.ReadUInt32();
        chrregnum = reader.ReadUInt32();
        slot = reader.ReadByte();
        Guild = new GUILD_CLIENT[chrregnum];
        for (int i = 0; i < chrregnum; i++)
        {
            Guild[i] = new GUILD_CLIENT();
            Guild[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nGuildNo);
        writer.Write(chrregnum);
        writer.Write(slot);
        for (int i = 0; i < Guild.Length; i++)
            Guild[i].Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 19 | Full: 0x1413
/// sizeof = 7
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 19)]
public class PROTO_NC_AVATAR_GUILD_DATA_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public uint chrregnum;
    public byte slot;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        chrregnum = reader.ReadUInt32();
        slot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(chrregnum);
        writer.Write(slot);
    }
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_AVATAR_GUILD_DATA_REQ_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_GUILD_DATA_REQ req = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        req.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        req.Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 22 | Full: 0x1416
/// sizeof = 14
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 22)]
public class PROTO_NC_AVATAR_GUILD_MEMBER_DATA_ACK : IFiestaPacketBody
{
    public uint chrregnum;
    public byte slot;
    public uint nNo;
    public ushort Error;
    public byte PartMask;
    public ushort NumOfMember;
    public GUILD_MEMEBER_INFO[] MemberList = [];

    public void Read(BinaryReader reader)
    {
        chrregnum = reader.ReadUInt32();
        slot = reader.ReadByte();
        nNo = reader.ReadUInt32();
        Error = reader.ReadUInt16();
        PartMask = reader.ReadByte();
        NumOfMember = reader.ReadUInt16();
        MemberList = new GUILD_MEMEBER_INFO[NumOfMember];
        for (int i = 0; i < NumOfMember; i++)
        {
            MemberList[i] = new GUILD_MEMEBER_INFO();
            MemberList[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(chrregnum);
        writer.Write(slot);
        writer.Write(nNo);
        writer.Write(Error);
        writer.Write(PartMask);
        writer.Write(NumOfMember);
        for (int i = 0; i < MemberList.Length; i++)
            MemberList[i].Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 21 | Full: 0x1415
/// sizeof = 11
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 21)]
public class PROTO_NC_AVATAR_GUILD_MEMBER_DATA_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public uint chrregnum;
    public uint nNo;
    public byte slot;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        chrregnum = reader.ReadUInt32();
        nNo = reader.ReadUInt32();
        slot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(chrregnum);
        writer.Write(nNo);
        writer.Write(slot);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 16 | Full: 0x1410
/// sizeof = 23
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 16)]
public class PROTO_NC_AVATAR_RENAME_ACK : IFiestaPacketBody
{
    public byte SlotNo;
    public Name5 NewName = new();
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        SlotNo = reader.ReadByte();
        NewName.Read(reader);
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(SlotNo);
        NewName.Write(writer);
        writer.Write(Error);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 18 | Full: 0x1412
/// sizeof = 29
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 18)]
public class PROTO_NC_AVATAR_RENAME_DB_ACK : IFiestaPacketBody
{
    public NETPACKETHEADER NetPacketHeader = new();
    public uint nCharNo;
    public byte SlotNo;
    public Name5 NewName = new();
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        NetPacketHeader.Read(reader);
        nCharNo = reader.ReadUInt32();
        SlotNo = reader.ReadByte();
        NewName.Read(reader);
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        NetPacketHeader.Write(writer);
        writer.Write(nCharNo);
        writer.Write(SlotNo);
        NewName.Write(writer);
        writer.Write(Error);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 17 | Full: 0x1411
/// sizeof = 27
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 17)]
public class PROTO_NC_AVATAR_RENAME_DB_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER NetPacketHeader = new();
    public uint nCharNo;
    public byte SlotNo;
    public Name5 NewName = new();

    public void Read(BinaryReader reader)
    {
        NetPacketHeader.Read(reader);
        nCharNo = reader.ReadUInt32();
        SlotNo = reader.ReadByte();
        NewName.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        NetPacketHeader.Write(writer);
        writer.Write(nCharNo);
        writer.Write(SlotNo);
        NewName.Write(writer);
    }
}

/// <summary>
/// Department: Avatar (0x05) | Opcode: 15 | Full: 0x140F
/// sizeof = 21
/// </summary>
[FiestaOpcode(ProtocolCommand.Avatar, 15)]
public class PROTO_NC_AVATAR_RENAME_REQ : IFiestaPacketBody
{
    public byte SlotNo;
    public Name5 NewName = new();

    public void Read(BinaryReader reader)
    {
        SlotNo = reader.ReadByte();
        NewName.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(SlotNo);
        NewName.Write(writer);
    }
}

