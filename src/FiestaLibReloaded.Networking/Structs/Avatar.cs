// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 25</summary>
public class PROTO_NC_AVATAR_CREATE_REQ
{
    public byte slotnum;
    public Name5 name = new();
    public PROTO_AVATAR_SHAPE_INFO char_shape = new();

    public void Read(BinaryReader reader)
    {
        slotnum = reader.ReadByte();
        name.Read(reader);
        char_shape.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(slotnum);
        name.Write(writer);
        char_shape.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 33</summary>
public class PROTO_NC_AVATAR_CREATEDATA_REQ
{
    public NETPACKETHEADER netpacketheader = new();
    public uint usrregnum;
    public byte createworld;
    public byte adminlevel;
    public PROTO_NC_AVATAR_CREATE_REQ clientdata = new();

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        usrregnum = reader.ReadUInt32();
        createworld = reader.ReadByte();
        adminlevel = reader.ReadByte();
        clientdata.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(usrregnum);
        writer.Write(createworld);
        writer.Write(adminlevel);
        clientdata.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 36</summary>
public class PROTO_NC_AVATAR_CREATEDATA_REQ_SEND
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_CREATEDATA_REQ req = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        req.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        req.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_AVATAR_CREATEDATAFAIL_ACK
{
    public Name5 charid = new();
    public ushort err;

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_AVATAR_CREATEDATASUC_ACK
{
    public byte numofavatar;
    public PROTO_AVATARINFORMATION[] avatar = [];

    public void Read(BinaryReader reader)
    {
        numofavatar = reader.ReadByte();
        avatar = new PROTO_AVATARINFORMATION[numofavatar];
        for (int i = 0; i < numofavatar; i++)
        {
            avatar[i] = new PROTO_AVATARINFORMATION();
            avatar[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(numofavatar);
        for (int i = 0; i < avatar.Length; i++)
            avatar[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_AVATAR_CREATEFAIL_ACK
{
    public ushort err;

    public void Read(BinaryReader reader)
    {
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 5</summary>
public class PROTO_NC_AVATAR_CREATEFAIL_ACK_SEND
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_CREATEFAIL_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 131</summary>
public class PROTO_NC_AVATAR_CREATESUCC_ACK
{
    public byte numofavatar;
    public PROTO_AVATARINFORMATION avatar = new();

    public void Read(BinaryReader reader)
    {
        numofavatar = reader.ReadByte();
        avatar.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(numofavatar);
        avatar.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 134</summary>
public class PROTO_NC_AVATAR_CREATESUCC_ACK_SEND
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_CREATESUCC_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_AVATAR_ERASE_REQ
{
    public byte slot;

    public void Read(BinaryReader reader)
    {
        slot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(slot);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 15</summary>
public class PROTO_NC_AVATAR_ERASEDATA_REQ
{
    public NETPACKETHEADER netpacketheader = new();
    public uint usrregnum;
    public uint avatar;
    public byte slot;
    public uint nNo;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        usrregnum = reader.ReadUInt32();
        avatar = reader.ReadUInt32();
        slot = reader.ReadByte();
        nNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(usrregnum);
        writer.Write(avatar);
        writer.Write(slot);
        writer.Write(nNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 18</summary>
public class PROTO_NC_AVATAR_ERASEDATA_REQ_SEND
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_ERASEDATA_REQ req = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        req.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        req.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_AVATAR_ERASEDATAFAIL_ACK
{
    public NETPACKETHEADER netpacketheader = new();
    public ushort err;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 15</summary>
public class PROTO_NC_AVATAR_ERASEDATASUC_ACK
{
    public NETPACKETHEADER netpacketheader = new();
    public uint usrregnum;
    public uint avatar;
    public byte slot;
    public uint nNo;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        usrregnum = reader.ReadUInt32();
        avatar = reader.ReadUInt32();
        slot = reader.ReadByte();
        nNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(usrregnum);
        writer.Write(avatar);
        writer.Write(slot);
        writer.Write(nNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_AVATAR_ERASEFAIL_ACK
{
    public ushort err;

    public void Read(BinaryReader reader)
    {
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 5</summary>
public class PROTO_NC_AVATAR_ERASEFAIL_ACK_SEND
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_ERASEFAIL_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_AVATAR_ERASESUC_ACK
{
    public byte slot;

    public void Read(BinaryReader reader)
    {
        slot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(slot);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_AVATAR_ERASESUC_ACK_SEND
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_ERASESUC_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 9</summary>
public class PROTO_NC_AVATAR_GUILD_DATA_ACK
{
    public uint nGuildNo;
    public uint chrregnum;
    public byte slot;
    public GUILD_CLIENT[] Guild = [];

    public void Read(BinaryReader reader)
    {
        nGuildNo = reader.ReadUInt32();
        chrregnum = reader.ReadUInt32();
        slot = reader.ReadByte();
        Guild = new GUILD_CLIENT[chrregnum];
        for (int i = 0; i < chrregnum; i++)
        {
            Guild[i] = new GUILD_CLIENT();
            Guild[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nGuildNo);
        writer.Write(chrregnum);
        writer.Write(slot);
        for (int i = 0; i < Guild.Length; i++)
            Guild[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 7</summary>
public class PROTO_NC_AVATAR_GUILD_DATA_REQ
{
    public NETPACKETHEADER netpacketheader = new();
    public uint chrregnum;
    public byte slot;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        chrregnum = reader.ReadUInt32();
        slot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(chrregnum);
        writer.Write(slot);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_AVATAR_GUILD_DATA_REQ_SEND
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_AVATAR_GUILD_DATA_REQ req = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        req.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        req.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 14</summary>
public class PROTO_NC_AVATAR_GUILD_MEMBER_DATA_ACK
{
    public uint chrregnum;
    public byte slot;
    public uint nNo;
    public ushort Error;
    public byte PartMask;
    public ushort NumOfMember;
    public GUILD_MEMEBER_INFO[] MemberList = [];

    public void Read(BinaryReader reader)
    {
        chrregnum = reader.ReadUInt32();
        slot = reader.ReadByte();
        nNo = reader.ReadUInt32();
        Error = reader.ReadUInt16();
        PartMask = reader.ReadByte();
        NumOfMember = reader.ReadUInt16();
        MemberList = new GUILD_MEMEBER_INFO[NumOfMember];
        for (int i = 0; i < NumOfMember; i++)
        {
            MemberList[i] = new GUILD_MEMEBER_INFO();
            MemberList[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(chrregnum);
        writer.Write(slot);
        writer.Write(nNo);
        writer.Write(Error);
        writer.Write(PartMask);
        writer.Write(NumOfMember);
        for (int i = 0; i < MemberList.Length; i++)
            MemberList[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 11</summary>
public class PROTO_NC_AVATAR_GUILD_MEMBER_DATA_REQ
{
    public NETPACKETHEADER netpacketheader = new();
    public uint chrregnum;
    public uint nNo;
    public byte slot;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        chrregnum = reader.ReadUInt32();
        nNo = reader.ReadUInt32();
        slot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(chrregnum);
        writer.Write(nNo);
        writer.Write(slot);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 23</summary>
public class PROTO_NC_AVATAR_RENAME_ACK
{
    public byte SlotNo;
    public Name5 NewName = new();
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        SlotNo = reader.ReadByte();
        NewName.Read(reader);
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(SlotNo);
        NewName.Write(writer);
        writer.Write(Error);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 29</summary>
public class PROTO_NC_AVATAR_RENAME_DB_ACK
{
    public NETPACKETHEADER NetPacketHeader = new();
    public uint nCharNo;
    public byte SlotNo;
    public Name5 NewName = new();
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        NetPacketHeader.Read(reader);
        nCharNo = reader.ReadUInt32();
        SlotNo = reader.ReadByte();
        NewName.Read(reader);
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        NetPacketHeader.Write(writer);
        writer.Write(nCharNo);
        writer.Write(SlotNo);
        NewName.Write(writer);
        writer.Write(Error);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 27</summary>
public class PROTO_NC_AVATAR_RENAME_DB_REQ
{
    public NETPACKETHEADER NetPacketHeader = new();
    public uint nCharNo;
    public byte SlotNo;
    public Name5 NewName = new();

    public void Read(BinaryReader reader)
    {
        NetPacketHeader.Read(reader);
        nCharNo = reader.ReadUInt32();
        SlotNo = reader.ReadByte();
        NewName.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        NetPacketHeader.Write(writer);
        writer.Write(nCharNo);
        writer.Write(SlotNo);
        NewName.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 21</summary>
public class PROTO_NC_AVATAR_RENAME_REQ
{
    public byte SlotNo;
    public Name5 NewName = new();

    public void Read(BinaryReader reader)
    {
        SlotNo = reader.ReadByte();
        NewName.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(SlotNo);
        NewName.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

