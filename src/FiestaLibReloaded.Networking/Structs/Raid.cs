// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_CHAT_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_RAID_CHAT_BROAD_CMD
{
    public byte itemLinkDataCount;
    public Name5 sTalkerCharID = new();
    public byte nChatLen;
    public byte[] sChat = [];

    public void Read(BinaryReader reader)
    {
        itemLinkDataCount = reader.ReadByte();
        sTalkerCharID.Read(reader);
        nChatLen = reader.ReadByte();
        sChat = reader.ReadBytes(itemLinkDataCount);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(itemLinkDataCount);
        sTalkerCharID.Write(writer);
        writer.Write(nChatLen);
        writer.Write(sChat);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_CHAT_REQ
{
    public byte itemLinkDataCount;
    public byte nChatLen;
    public byte[] sChat = [];

    public void Read(BinaryReader reader)
    {
        itemLinkDataCount = reader.ReadByte();
        nChatLen = reader.ReadByte();
        sChat = reader.ReadBytes(itemLinkDataCount);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(itemLinkDataCount);
        writer.Write(nChatLen);
        writer.Write(sChat);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_RAID_DEL_SUB_LEADER_ACK
{
    public Name5 sDelSubLeaderCharID = new();
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        sDelSubLeaderCharID.Read(reader);
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sDelSubLeaderCharID.Write(writer);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 44</summary>
public class PROTO_NC_RAID_DEL_SUB_LEADER_BROAD_CMD
{
    public Name5 sCommandCharID = new();
    public Name5 sDelSubLeaderCharID = new();
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
        sDelSubLeaderCharID.Read(reader);
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
        sDelSubLeaderCharID.Write(writer);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_DEL_SUB_LEADER_CMD
{
    public Name5 sCommandCharID = new();

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_DEL_SUB_LEADER_REQ
{
    public Name5 sDelSubLeaderCharID = new();

    public void Read(BinaryReader reader)
    {
        sDelSubLeaderCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sDelSubLeaderCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_RAID_DISMISS_BROAD_CMD
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_RAID_INVITATION_ACK
{
    public Name5 sInvitationCharID = new();
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        sInvitationCharID.Read(reader);
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sInvitationCharID.Write(writer);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_INVITATION_ANSWER_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 21</summary>
public class PROTO_NC_RAID_INVITATION_ANSWER_CMD
{
    public Name5 sInvitationCharID = new();
    public byte bIsAllow;

    public void Read(BinaryReader reader)
    {
        sInvitationCharID.Read(reader);
        bIsAllow = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        sInvitationCharID.Write(writer);
        writer.Write(bIsAllow);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 21</summary>
public class PROTO_NC_RAID_INVITATION_ANSWER_REQ
{
    public byte bIsAllow;
    public Name5 sMasterCharID = new();

    public void Read(BinaryReader reader)
    {
        bIsAllow = reader.ReadByte();
        sMasterCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bIsAllow);
        sMasterCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_INVITATION_CMD
{
    public Name5 sMasterCharID = new();

    public void Read(BinaryReader reader)
    {
        sMasterCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sMasterCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_INVITATION_REQ
{
    public Name5 sInvitationCharID = new();

    public void Read(BinaryReader reader)
    {
        sInvitationCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sInvitationCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_LEAVE_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 24</summary>
public class PROTO_NC_RAID_LEAVE_BROAD_CMD
{
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;
    public Name5 sCharID = new();

    public void Read(BinaryReader reader)
    {
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
        sCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
        sCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_RAID_LOCATION_CMD
{
    public byte nNum;
    // PROTO_NC_RAID_LOCATION_CMD::Memberinfo[0] RaidMembers at offset 1 (unsupported type)

    public void Read(BinaryReader reader)
    {
        nNum = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nNum);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_RAID_LOGIN_CMD
{
    public ushort nRaidNo;
    public uint nCharNo;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nCharNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nCharNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 24</summary>
public class PROTO_NC_RAID_LOGININFO_CMD
{
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;
    public Name5 sCharID = new();

    public void Read(BinaryReader reader)
    {
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
        sCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
        sCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_RAID_LOGOUT_CMD
{
    public ushort nRaidNo;
    public uint nCharNo;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nCharNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nCharNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 24</summary>
public class PROTO_NC_RAID_LOGOUTINFO_CMD
{
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;
    public Name5 sCharID = new();

    public void Read(BinaryReader reader)
    {
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
        sCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
        sCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_LOOTING_TYPE_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_LOOTING_TYPE_BROAD_CMD
{
    public byte nItemGrade;
    public byte nLootingType;

    public void Read(BinaryReader reader)
    {
        nItemGrade = reader.ReadByte();
        nLootingType = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nItemGrade);
        writer.Write(nLootingType);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_LOOTING_TYPE_REQ
{
    public byte nItemGrade;
    public byte nLootingType;

    public void Read(BinaryReader reader)
    {
        nItemGrade = reader.ReadByte();
        nLootingType = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nItemGrade);
        writer.Write(nLootingType);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_RAID_MAKE_ACK
{
    public ushort nRaidNo;
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 110</summary>
public class PROTO_NC_RAID_MAKE_BROAD_CMD
{
    public ushort nRaidNo;
    public ushort[] nRaidPartyNo = new ushort[4];
    public Name5[] sFirstPartyCharID = new Name5[5];

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        for (int i = 0; i < 4; i++)
            nRaidPartyNo[i] = reader.ReadUInt16();
        for (int i = 0; i < 5; i++)
        {
            sFirstPartyCharID[i] = new Name5();
            sFirstPartyCharID[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        for (int i = 0; i < 4; i++)
            writer.Write(nRaidPartyNo[i]);
        for (int i = 0; i < 5; i++)
            sFirstPartyCharID[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_RAID_MAKE_REQ
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_MAPOUT_CMD
{
    public Name5 sCharID = new();

    public void Read(BinaryReader reader)
    {
        sCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 24</summary>
public class PROTO_NC_RAID_MEMBER_JOIN_BROAD_CMD
{
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;
    public Name5 sCharID = new();

    public void Read(BinaryReader reader)
    {
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
        sCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
        sCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 542</summary>
public class PROTO_NC_RAID_MEMBER_LIST_CMD
{
    public Name5 Leaderid = new();
    public Name5[] SubLeaderid = new Name5[4];
    public Name5 Looterid = new();
    public byte nItemGrade;
    public byte nLootingType;
    // PROTO_NC_RAID_MEMBER_LIST_CMD::MEMBER_INFO[20] memberarray at offset 122 (unsupported type)

    public void Read(BinaryReader reader)
    {
        Leaderid.Read(reader);
        for (int i = 0; i < 4; i++)
        {
            SubLeaderid[i] = new Name5();
            SubLeaderid[i].Read(reader);
        }
        Looterid.Read(reader);
        nItemGrade = reader.ReadByte();
        nLootingType = reader.ReadByte();
        reader.ReadBytes(420); // skip PROTO_NC_RAID_MEMBER_LIST_CMD::MEMBER_INFO[20] memberarray
    }

    public void Write(BinaryWriter writer)
    {
        Leaderid.Write(writer);
        for (int i = 0; i < 4; i++)
            SubLeaderid[i].Write(writer);
        Looterid.Write(writer);
        writer.Write(nItemGrade);
        writer.Write(nLootingType);
        writer.Write(new byte[420]); // skip PROTO_NC_RAID_MEMBER_LIST_CMD::MEMBER_INFO[20] memberarray
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_RAID_MEMBERCLASS_CMD
{
    public byte nNum;
    public RAID_MEMBERCLASS[] RaidMembers = [];

    public void Read(BinaryReader reader)
    {
        nNum = reader.ReadByte();
        RaidMembers = new RAID_MEMBERCLASS[nNum];
        for (int i = 0; i < nNum; i++)
        {
            RaidMembers[i] = new RAID_MEMBERCLASS();
            RaidMembers[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nNum);
        for (int i = 0; i < RaidMembers.Length; i++)
            RaidMembers[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 16</summary>
public class PROTO_NC_RAID_MEMBERINFORM_CMD
{
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;
    public uint hp;
    public uint sp;
    public uint lp;

    public void Read(BinaryReader reader)
    {
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
        hp = reader.ReadUInt32();
        sp = reader.ReadUInt32();
        lp = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
        writer.Write(hp);
        writer.Write(sp);
        writer.Write(lp);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_RAID_SET_LEADER_ACK
{
    public Name5 sNewLeaderCharID = new();
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        sNewLeaderCharID.Read(reader);
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sNewLeaderCharID.Write(writer);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 44</summary>
public class PROTO_NC_RAID_SET_LEADER_BROAD_CMD
{
    public Name5 sCommandCharID = new();
    public Name5 sNewLeaderCharID = new();
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
        sNewLeaderCharID.Read(reader);
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
        sNewLeaderCharID.Write(writer);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_SET_LEADER_CMD
{
    public Name5 sCommandCharID = new();

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_SET_LEADER_REQ
{
    public Name5 sNewLeaderCharID = new();

    public void Read(BinaryReader reader)
    {
        sNewLeaderCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sNewLeaderCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_RAID_SET_LOOTER_ACK
{
    public Name5 sNewLooterCharID = new();
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        sNewLooterCharID.Read(reader);
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sNewLooterCharID.Write(writer);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 44</summary>
public class PROTO_NC_RAID_SET_LOOTER_BROAD_CMD
{
    public Name5 sCommandCharID = new();
    public Name5 sNewLooterCharID = new();
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
        sNewLooterCharID.Read(reader);
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
        sNewLooterCharID.Write(writer);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_SET_LOOTER_CMD
{
    public Name5 sCommandCharID = new();

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_SET_LOOTER_REQ
{
    public Name5 sNewLooterCharID = new();

    public void Read(BinaryReader reader)
    {
        sNewLooterCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sNewLooterCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_RAID_SET_SUB_LEADER_ACK
{
    public Name5 sNewSubLeaderCharID = new();
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        sNewSubLeaderCharID.Read(reader);
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sNewSubLeaderCharID.Write(writer);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 44</summary>
public class PROTO_NC_RAID_SET_SUB_LEADER_BROAD_CMD
{
    public Name5 sCommandCharID = new();
    public Name5 sNewSubLeaderCharID = new();
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
        sNewSubLeaderCharID.Read(reader);
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
        sNewSubLeaderCharID.Write(writer);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_SET_SUB_LEADER_CMD
{
    public Name5 sCommandCharID = new();

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_SET_SUB_LEADER_REQ
{
    public Name5 sNewSubLeaderCharID = new();

    public void Read(BinaryReader reader)
    {
        sNewSubLeaderCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sNewSubLeaderCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_RAID_SLOT_MOVE_ACK
{
    public Name5 sSlotMoveCharID = new();
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        sSlotMoveCharID.Read(reader);
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sSlotMoveCharID.Write(writer);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 228</summary>
public class PROTO_NC_RAID_SLOT_MOVE_BROAD_CMD
{
    public Name5 sCommandCharID = new();
    // PROTO_NC_RAID_SLOT_MOVE_BROAD_CMD::<unnamed-type-PrePosition> PrePosition at offset 20 (unsupported type)
    // PROTO_NC_RAID_SLOT_MOVE_BROAD_CMD::<unnamed-type-PostPosition> PostPosition at offset 24 (unsupported type)
    public Name5[] sPreCharID = new Name5[5];
    public Name5[] sPostCharID = new Name5[5];

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
        reader.ReadBytes(4); // skip PROTO_NC_RAID_SLOT_MOVE_BROAD_CMD::<unnamed-type-PrePosition> PrePosition
        reader.ReadBytes(4); // skip PROTO_NC_RAID_SLOT_MOVE_BROAD_CMD::<unnamed-type-PostPosition> PostPosition
        for (int i = 0; i < 5; i++)
        {
            sPreCharID[i] = new Name5();
            sPreCharID[i].Read(reader);
        }
        for (int i = 0; i < 5; i++)
        {
            sPostCharID[i] = new Name5();
            sPostCharID[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
        writer.Write(new byte[4]); // skip PROTO_NC_RAID_SLOT_MOVE_BROAD_CMD::<unnamed-type-PrePosition> PrePosition
        writer.Write(new byte[4]); // skip PROTO_NC_RAID_SLOT_MOVE_BROAD_CMD::<unnamed-type-PostPosition> PostPosition
        for (int i = 0; i < 5; i++)
            sPreCharID[i].Write(writer);
        for (int i = 0; i < 5; i++)
            sPostCharID[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 48</summary>
public class PROTO_NC_RAID_SLOT_MOVE_REQ
{
    // PROTO_NC_RAID_SLOT_MOVE_REQ::<unnamed-type-PrePosition> PrePosition at offset 0 (unsupported type)
    // PROTO_NC_RAID_SLOT_MOVE_REQ::<unnamed-type-PostPosition> PostPosition at offset 4 (unsupported type)
    public Name5 sPreSlotMoveCharID = new();
    public Name5 sPostSlotMoveCharID = new();

    public void Read(BinaryReader reader)
    {
        reader.ReadBytes(4); // skip PROTO_NC_RAID_SLOT_MOVE_REQ::<unnamed-type-PrePosition> PrePosition
        reader.ReadBytes(4); // skip PROTO_NC_RAID_SLOT_MOVE_REQ::<unnamed-type-PostPosition> PostPosition
        sPreSlotMoveCharID.Read(reader);
        sPostSlotMoveCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(new byte[4]); // skip PROTO_NC_RAID_SLOT_MOVE_REQ::<unnamed-type-PrePosition> PrePosition
        writer.Write(new byte[4]); // skip PROTO_NC_RAID_SLOT_MOVE_REQ::<unnamed-type-PostPosition> PostPosition
        sPreSlotMoveCharID.Write(writer);
        sPostSlotMoveCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_RAID_VANISH_ACK
{
    public Name5 sVanishCharID = new();
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        sVanishCharID.Read(reader);
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sVanishCharID.Write(writer);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 44</summary>
public class PROTO_NC_RAID_VANISH_BROAD_CMD
{
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;
    public Name5 sCommandCharID = new();
    public Name5 sVanishCharID = new();

    public void Read(BinaryReader reader)
    {
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
        sCommandCharID.Read(reader);
        sVanishCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
        sCommandCharID.Write(writer);
        sVanishCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_VANISH_CMD
{
    public Name5 sCommandCharID = new();

    public void Read(BinaryReader reader)
    {
        sCommandCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sCommandCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_RAID_VANISH_REQ
{
    public Name5 sVanishCharID = new();

    public void Read(BinaryReader reader)
    {
        sVanishCharID.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sVanishCharID.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_WARNING_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_RAID_WARNING_BROAD_CMD
{
    public byte itemLinkDataCount;
    public Name5 sTalkerCharID = new();
    public byte nChatLen;
    public byte[] sChat = [];

    public void Read(BinaryReader reader)
    {
        itemLinkDataCount = reader.ReadByte();
        sTalkerCharID.Read(reader);
        nChatLen = reader.ReadByte();
        sChat = reader.ReadBytes(itemLinkDataCount);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(itemLinkDataCount);
        sTalkerCharID.Write(writer);
        writer.Write(nChatLen);
        writer.Write(sChat);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_WARNING_REQ
{
    public byte itemLinkDataCount;
    public byte nChatLen;
    public byte[] sChat = [];

    public void Read(BinaryReader reader)
    {
        itemLinkDataCount = reader.ReadByte();
        nChatLen = reader.ReadByte();
        sChat = reader.ReadBytes(itemLinkDataCount);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(itemLinkDataCount);
        writer.Write(nChatLen);
        writer.Write(sChat);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_RAID_ZONE_DEL_SUB_LEADER_CMD
{
    public ushort nRaidNo;
    public uint nDelLeaderCharNo;
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nDelLeaderCharNo = reader.ReadUInt32();
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nDelLeaderCharNo);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_RAID_ZONE_DISMISS_CMD
{
    public ushort nRaidNo;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_RAID_ZONE_LEAVE_CMD
{
    public ushort nRaidNo;
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;
    public uint nCharNo;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
        nCharNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
        writer.Write(nCharNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_RAID_ZONE_LOOTING_TYPE_CMD
{
    public ushort nRaidNo;
    public byte nItemGrade;
    public byte nLootingType;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nItemGrade = reader.ReadByte();
        nLootingType = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nItemGrade);
        writer.Write(nLootingType);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 32</summary>
public class PROTO_NC_RAID_ZONE_MAKE_CMD
{
    public ushort nPartyNo;
    public ushort nRaidNo;
    public ushort[] nRaidPartyNo = new ushort[4];
    public uint[] FirstPartyCharRegNo = new uint[5];

    public void Read(BinaryReader reader)
    {
        nPartyNo = reader.ReadUInt16();
        nRaidNo = reader.ReadUInt16();
        for (int i = 0; i < 4; i++)
            nRaidPartyNo[i] = reader.ReadUInt16();
        for (int i = 0; i < 5; i++)
            FirstPartyCharRegNo[i] = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPartyNo);
        writer.Write(nRaidNo);
        for (int i = 0; i < 4; i++)
            writer.Write(nRaidPartyNo[i]);
        for (int i = 0; i < 5; i++)
            writer.Write(FirstPartyCharRegNo[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 42</summary>
public class PROTO_NC_RAID_ZONE_MEMBER_JOIN_CMD
{
    public ushort nRaidNo;
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;
    public uint nCharNo;
    public PROTO_PARTY_MEMBER_AND_ZONEMERCHINE tInfo = new();

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
        nCharNo = reader.ReadUInt32();
        tInfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
        writer.Write(nCharNo);
        tInfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_RAID_ZONE_SET_LEADER_CMD
{
    public ushort nRaidNo;
    public uint nNewLeaderCharNo;
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nNewLeaderCharNo = reader.ReadUInt32();
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nNewLeaderCharNo);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_RAID_ZONE_SET_LOOTER_CMD
{
    public ushort nRaidNo;
    public uint nNewLooterCharNo;
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nNewLooterCharNo = reader.ReadUInt32();
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nNewLooterCharNo);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_RAID_ZONE_SET_SUB_LEADER_CMD
{
    public ushort nRaidNo;
    public uint nNewSubLeaderCharNo;
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nNewSubLeaderCharNo = reader.ReadUInt32();
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nNewSubLeaderCharNo);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 50</summary>
public class PROTO_NC_RAID_ZONE_SLOT_MOVE_CMD
{
    public ushort nRaidNo;
    // PROTO_NC_RAID_ZONE_SLOT_MOVE_CMD::<unnamed-type-PrePosition> PrePosition at offset 2 (unsupported type)
    // PROTO_NC_RAID_ZONE_SLOT_MOVE_CMD::<unnamed-type-PostPosition> PostPosition at offset 26 (unsupported type)

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        reader.ReadBytes(24); // skip PROTO_NC_RAID_ZONE_SLOT_MOVE_CMD::<unnamed-type-PrePosition> PrePosition
        reader.ReadBytes(24); // skip PROTO_NC_RAID_ZONE_SLOT_MOVE_CMD::<unnamed-type-PostPosition> PostPosition
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(new byte[24]); // skip PROTO_NC_RAID_ZONE_SLOT_MOVE_CMD::<unnamed-type-PrePosition> PrePosition
        writer.Write(new byte[24]); // skip PROTO_NC_RAID_ZONE_SLOT_MOVE_CMD::<unnamed-type-PostPosition> PostPosition
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_RAID_ZONE_VANISH_CMD
{
    public ushort nRaidNo;
    public ushort nPartyNo;
    public byte nPartyIndex;
    public byte nSlotIndex;
    public uint nVanishCharNo;

    public void Read(BinaryReader reader)
    {
        nRaidNo = reader.ReadUInt16();
        nPartyNo = reader.ReadUInt16();
        nPartyIndex = reader.ReadByte();
        nSlotIndex = reader.ReadByte();
        nVanishCharNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nRaidNo);
        writer.Write(nPartyNo);
        writer.Write(nPartyIndex);
        writer.Write(nSlotIndex);
        writer.Write(nVanishCharNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

