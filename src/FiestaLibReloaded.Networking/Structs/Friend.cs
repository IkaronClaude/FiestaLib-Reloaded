// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 72</summary>
public class PROTO_NC_FRIEND_ADD_CMD
{
    public PROTO_FRIEND_INFO friendinfo = new();

    public void Read(BinaryReader reader)
    {
        friendinfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        friendinfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 21</summary>
public class PROTO_NC_FRIEND_CLASS_CHANGE_CMD
{
    public Name5 charid = new();
    public byte new_class;

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        new_class = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        writer.Write(new_class);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 48</summary>
public class PROTO_NC_FRIEND_DB_DEL_ACK
{
    public ushort handle_charno;
    public uint charno;
    public Name5 charid = new();
    public Name5 friendid = new();
    public ushort err;

    public void Read(BinaryReader reader)
    {
        handle_charno = reader.ReadUInt16();
        charno = reader.ReadUInt32();
        charid.Read(reader);
        friendid.Read(reader);
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(handle_charno);
        writer.Write(charno);
        charid.Write(writer);
        friendid.Write(writer);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 46</summary>
public class PROTO_NC_FRIEND_DB_DEL_REQ
{
    public ushort handle_charno;
    public uint charno;
    public Name5 charid = new();
    public Name5 friendid = new();

    public void Read(BinaryReader reader)
    {
        handle_charno = reader.ReadUInt16();
        charno = reader.ReadUInt32();
        charid.Read(reader);
        friendid.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(handle_charno);
        writer.Write(charno);
        charid.Write(writer);
        friendid.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 84</summary>
public class PROTO_NC_FRIEND_DB_GET_ACK
{
    public ushort handle_char;
    public uint charno;
    public uint friendno;
    public ushort err;
    public PROTO_FRIEND_INFO friendinfo = new();

    public void Read(BinaryReader reader)
    {
        handle_char = reader.ReadUInt16();
        charno = reader.ReadUInt32();
        friendno = reader.ReadUInt32();
        err = reader.ReadUInt16();
        friendinfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(handle_char);
        writer.Write(charno);
        writer.Write(friendno);
        writer.Write(err);
        friendinfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_FRIEND_DB_GET_REQ
{
    public ushort handle_char;
    public uint charno;
    public uint friendno;

    public void Read(BinaryReader reader)
    {
        handle_char = reader.ReadUInt16();
        charno = reader.ReadUInt32();
        friendno = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(handle_char);
        writer.Write(charno);
        writer.Write(friendno);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 8</summary>
public class PROTO_NC_FRIEND_DB_POINT
{
    public uint charregnum;
    public ushort friendpoint;
    public ushort diffpoint;

    public void Read(BinaryReader reader)
    {
        charregnum = reader.ReadUInt32();
        friendpoint = reader.ReadUInt16();
        diffpoint = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(charregnum);
        writer.Write(friendpoint);
        writer.Write(diffpoint);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_FRIEND_DB_POINT_CMD
{
    public ushort numoffriend;
    public PROTO_NC_FRIEND_DB_POINT[] pointinfo = [];

    public void Read(BinaryReader reader)
    {
        numoffriend = reader.ReadUInt16();
        pointinfo = new PROTO_NC_FRIEND_DB_POINT[numoffriend];
        for (int i = 0; i < numoffriend; i++)
        {
            pointinfo[i] = new PROTO_NC_FRIEND_DB_POINT();
            pointinfo[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(numoffriend);
        for (int i = 0; i < pointinfo.Length; i++)
            pointinfo[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 14</summary>
public class PROTO_NC_FRIEND_DB_SET_ACK
{
    public ushort handle_charno;
    public ushort handle_friendno;
    public uint charno;
    public uint friendno;
    public ushort err;

    public void Read(BinaryReader reader)
    {
        handle_charno = reader.ReadUInt16();
        handle_friendno = reader.ReadUInt16();
        charno = reader.ReadUInt32();
        friendno = reader.ReadUInt32();
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(handle_charno);
        writer.Write(handle_friendno);
        writer.Write(charno);
        writer.Write(friendno);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 13</summary>
public class PROTO_NC_FRIEND_DB_SET_REQ
{
    public ushort handle_charno;
    public ushort handle_friendno;
    public uint charno;
    public uint friendno;
    public byte flag;

    public void Read(BinaryReader reader)
    {
        handle_charno = reader.ReadUInt16();
        handle_friendno = reader.ReadUInt16();
        charno = reader.ReadUInt32();
        friendno = reader.ReadUInt32();
        flag = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(handle_charno);
        writer.Write(handle_friendno);
        writer.Write(charno);
        writer.Write(friendno);
        writer.Write(flag);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 42</summary>
public class PROTO_NC_FRIEND_DEL_ACK
{
    public Name5 charid = new();
    public Name5 friendid = new();
    public ushort err;

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        friendid.Read(reader);
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        friendid.Write(writer);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_FRIEND_DEL_CMD
{
    public Name5 charid = new();

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 40</summary>
public class PROTO_NC_FRIEND_DEL_REQ
{
    public Name5 charid = new();
    public Name5 friendid = new();

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        friendid.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        friendid.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_FRIEND_FIND_FRIENDS_ACK
{
    public ushort error;
    public ushort numoffriend;
    public PROTO_FRIEND_INFO[] friendinfo = [];

    public void Read(BinaryReader reader)
    {
        error = reader.ReadUInt16();
        numoffriend = reader.ReadUInt16();
        friendinfo = new PROTO_FRIEND_INFO[numoffriend];
        for (int i = 0; i < numoffriend; i++)
        {
            friendinfo[i] = new PROTO_FRIEND_INFO();
            friendinfo[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(error);
        writer.Write(numoffriend);
        for (int i = 0; i < friendinfo.Length; i++)
            friendinfo[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_FRIEND_GET_DIFF_FRIEND_POINT_CMD
{
    public ushort diffpoint;

    public void Read(BinaryReader reader)
    {
        diffpoint = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(diffpoint);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 21</summary>
public class PROTO_NC_FRIEND_LEVEL_CMD
{
    public Name5 charid = new();
    public byte level;

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        level = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        writer.Write(level);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_FRIEND_LIST_CMD
{
    public byte numoffriend;
    public PROTO_FRIEND_INFO[] friendinfo = [];

    public void Read(BinaryReader reader)
    {
        numoffriend = reader.ReadByte();
        friendinfo = new PROTO_FRIEND_INFO[numoffriend];
        for (int i = 0; i < numoffriend; i++)
        {
            friendinfo[i] = new PROTO_FRIEND_INFO();
            friendinfo[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(numoffriend);
        for (int i = 0; i < friendinfo.Length; i++)
            friendinfo[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 32</summary>
public class PROTO_NC_FRIEND_LOGIN_CMD
{
    public Name5 charid = new();
    public Name3 map = new();

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        map.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        map.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_FRIEND_LOGOUT_CMD
{
    public Name5 charid = new();

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 32</summary>
public class PROTO_NC_FRIEND_MAP_CMD
{
    public Name5 charid = new();
    public Name3 map = new();

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        map.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        map.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 21</summary>
public class PROTO_NC_FRIEND_PARTY_CMD
{
    public Name5 charid = new();
    public byte is_party;

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        is_party = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        writer.Write(is_party);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_FRIEND_POINT_ACK
{
    public ushort friendpoint;

    public void Read(BinaryReader reader)
    {
        friendpoint = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(friendpoint);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_FRIEND_REFUSE_CMD
{
    public Name5 friendid = new();

    public void Read(BinaryReader reader)
    {
        friendid.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        friendid.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 42</summary>
public class PROTO_NC_FRIEND_SET_ACK
{
    public Name5 charid = new();
    public Name5 friendid = new();
    public ushort err;

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        friendid.Read(reader);
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        friendid.Write(writer);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 41</summary>
public class PROTO_NC_FRIEND_SET_CONFIRM_ACK
{
    public Name5 charid = new();
    public Name5 friendid = new();
    public byte accept_friend;

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        friendid.Read(reader);
        accept_friend = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        friendid.Write(writer);
        writer.Write(accept_friend);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 40</summary>
public class PROTO_NC_FRIEND_SET_CONFIRM_REQ
{
    public Name5 charid = new();
    public Name5 friendid = new();

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        friendid.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        friendid.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 40</summary>
public class PROTO_NC_FRIEND_SET_REQ
{
    public Name5 charid = new();
    public Name5 friendid = new();

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
        friendid.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
        friendid.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_FRIEND_SOMEONE_GET_SPECIALITEM_WORLD_CMD
{
    public Name5 charid = new();

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_FRIEND_SOMEONE_GET_SPECIALITEM_ZONE_CMD
{
    public Name5 charid = new();

    public void Read(BinaryReader reader)
    {
        charid.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        charid.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 7</summary>
public class PROTO_NC_FRIEND_UES_FRIEND_POINT_ACK
{
    public ushort error;
    public ushort friendpoint;
    public ushort itemid;
    public byte numofitem;

    public void Read(BinaryReader reader)
    {
        error = reader.ReadUInt16();
        friendpoint = reader.ReadUInt16();
        itemid = reader.ReadUInt16();
        numofitem = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(error);
        writer.Write(friendpoint);
        writer.Write(itemid);
        writer.Write(numofitem);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

