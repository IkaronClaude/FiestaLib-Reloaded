// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_ACTIV_REQ
{
    public byte slot;

    public void Read(BinaryReader reader)
    {
        slot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(slot);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_ARRANGEMODE_REQ
{
    public byte ArrangeMode;

    public void Read(BinaryReader reader)
    {
        ArrangeMode = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(ArrangeMode);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 66</summary>
public class PROTO_NC_MINIHOUSE_BUILDING_CMD
{
    public ushort player;
    public PROTO_NC_BRIEFINFO_MINIHOUSEBUILD_CMD minihouseinfo = new();

    public void Read(BinaryReader reader)
    {
        player = reader.ReadUInt16();
        minihouseinfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(player);
        minihouseinfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 133</summary>
public class PROTO_NC_MINIHOUSE_BUILDING_REQ
{
    public byte maxvisiter;
    public sbyte[] password = new sbyte[9];
    public sbyte[] title = new sbyte[21];
    public byte bItemInfoClose;
    public sbyte[] sNotify = new sbyte[101];

    public void Read(BinaryReader reader)
    {
        maxvisiter = reader.ReadByte();
        for (int i = 0; i < 9; i++)
            password[i] = reader.ReadSByte();
        for (int i = 0; i < 21; i++)
            title[i] = reader.ReadSByte();
        bItemInfoClose = reader.ReadByte();
        for (int i = 0; i < 101; i++)
            sNotify[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(maxvisiter);
        for (int i = 0; i < 9; i++)
            writer.Write(password[i]);
        for (int i = 0; i < 21; i++)
            writer.Write(title[i]);
        writer.Write(bItemInfoClose);
        for (int i = 0; i < 101; i++)
            writer.Write(sNotify[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_CHAR_ACTION_ACK
{
    public ushort err;

    public void Read(BinaryReader reader)
    {
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_MINIHOUSE_CHAR_ACTION_CMD
{
    public ushort PreHandle;
    public ushort NextHandle;
    public ushort nFurnitureHnd;
    public ushort ActorHnd;
    public byte ActorIdx;
    public byte ActeeIdx;

    public void Read(BinaryReader reader)
    {
        PreHandle = reader.ReadUInt16();
        NextHandle = reader.ReadUInt16();
        nFurnitureHnd = reader.ReadUInt16();
        ActorHnd = reader.ReadUInt16();
        ActorIdx = reader.ReadByte();
        ActeeIdx = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(PreHandle);
        writer.Write(NextHandle);
        writer.Write(nFurnitureHnd);
        writer.Write(ActorHnd);
        writer.Write(ActorIdx);
        writer.Write(ActeeIdx);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MINIHOUSE_CHAR_ACTION_REQ
{
    public ushort Handle;
    public ushort nFurnitureHnd;

    public void Read(BinaryReader reader)
    {
        Handle = reader.ReadUInt16();
        nFurnitureHnd = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(Handle);
        writer.Write(nFurnitureHnd);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_MINIHOUSE_COMPULSIONMOVETO_REQ
{
    public ushort hnd;
    public SHINE_XY_TYPE location = new();

    public void Read(BinaryReader reader)
    {
        hnd = reader.ReadUInt16();
        location.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(hnd);
        location.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_CREATE_FURNITURE_CMD
{
    public byte furniturecount;
    // PROTO_NC_MINIHOUSE_CREATE_FURNITURE_CMD::<unnamed-type-rearrange>[0] rearrange at offset 1 (unsupported type)

    public void Read(BinaryReader reader)
    {
        furniturecount = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(furniturecount);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_CREATE_FURNITURE_REQ
{
    public byte furniturecount;
    // PROTO_NC_MINIHOUSE_CREATE_FURNITURE_REQ::<unnamed-type-rearrange>[0] rearrange at offset 1 (unsupported type)

    public void Read(BinaryReader reader)
    {
        furniturecount = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(furniturecount);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 17</summary>
public class PROTO_NC_MINIHOUSE_DB_PORTAL_ADD_ACK
{
    public NETPACKETZONEHEADER header = new();
    public ushort nPortalItemID;
    public byte bType;
    public int dEndDate;
    public ushort lockindex;
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        nPortalItemID = reader.ReadUInt16();
        bType = reader.ReadByte();
        dEndDate = reader.ReadInt32();
        lockindex = reader.ReadUInt16();
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(nPortalItemID);
        writer.Write(bType);
        writer.Write(dEndDate);
        writer.Write(lockindex);
        writer.Write(Error);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 25</summary>
public class PROTO_NC_MINIHOUSE_DB_PORTAL_ADD_REQ
{
    public NETPACKETZONEHEADER header = new();
    public ushort nPortalItemID;
    public byte bType;
    public int dEndDate;
    public ushort lockindex;
    public PROTO_ITEM_RELOC Item = new();

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        nPortalItemID = reader.ReadUInt16();
        bType = reader.ReadByte();
        dEndDate = reader.ReadInt32();
        lockindex = reader.ReadUInt16();
        Item.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(nPortalItemID);
        writer.Write(bType);
        writer.Write(dEndDate);
        writer.Write(lockindex);
        Item.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 8</summary>
public class PROTO_NC_MINIHOUSE_DB_PORTAL_DEL_ACK
{
    public NETPACKETZONEHEADER header = new();
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(Error);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_MINIHOUSE_DB_PORTAL_LIST_ACK
{
    public NETPACKETZONEHEADER header = new();
    public ushort Error;
    public ushort nPortalCnt;
    public PORTAL_INFO[] PortalInfo = [];

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        Error = reader.ReadUInt16();
        nPortalCnt = reader.ReadUInt16();
        PortalInfo = new PORTAL_INFO[nPortalCnt];
        for (int i = 0; i < nPortalCnt; i++)
        {
            PortalInfo[i] = new PORTAL_INFO();
            PortalInfo[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(Error);
        writer.Write(nPortalCnt);
        for (int i = 0; i < PortalInfo.Length; i++)
            PortalInfo[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_MINIHOUSE_DB_PORTAL_LIST_REQ
{
    public NETPACKETZONEHEADER header = new();

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 8</summary>
public class PROTO_NC_MINIHOUSE_DB_VISITER_COUNT_ACK
{
    public NETPACKETZONEHEADER header = new();
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(Error);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_MINIHOUSE_DB_VISITER_COUNT_REQ
{
    public NETPACKETZONEHEADER header = new();
    public uint nVisiterCharNo;

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        nVisiterCharNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(nVisiterCharNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_DELETE_FURNITURE_REQ
{
    public byte furniturecount;
    public ushort[] furniturehandle = [];

    public void Read(BinaryReader reader)
    {
        furniturecount = reader.ReadByte();
        furniturehandle = new ushort[furniturecount];
        for (int i = 0; i < furniturecount; i++)
            furniturehandle[i] = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(furniturecount);
        for (int i = 0; i < furniturehandle.Length; i++)
            writer.Write(furniturehandle[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_FUNICHER_CMD
{
    public byte furniturecount;
    public sbyte next;
    // PROTO_NC_MINIHOUSE_FUNICHER_CMD::<unnamed-type-array>[0] array at offset 2 (unsupported type)

    public void Read(BinaryReader reader)
    {
        furniturecount = reader.ReadByte();
        next = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(furniturecount);
        writer.Write(next);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_FUNITUREINFOCOMPLETE_REQ
{
    public byte funitureinfocomplete;

    public void Read(BinaryReader reader)
    {
        funitureinfocomplete = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(funitureinfocomplete);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_EFFECT_ACK
{
    public ushort nEffectHandle;
    public ushort err;

    public void Read(BinaryReader reader)
    {
        nEffectHandle = reader.ReadUInt16();
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nEffectHandle);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_EFFECT_CMD
{
    public ushort nEffectHandle;
    public ushort nApplyPlayerCnt;
    public ushort[] nApplyPlayerHandle = [];

    public void Read(BinaryReader reader)
    {
        nEffectHandle = reader.ReadUInt16();
        nApplyPlayerCnt = reader.ReadUInt16();
        nApplyPlayerHandle = new ushort[nApplyPlayerCnt];
        for (int i = 0; i < nApplyPlayerCnt; i++)
            nApplyPlayerHandle[i] = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nEffectHandle);
        writer.Write(nApplyPlayerCnt);
        for (int i = 0; i < nApplyPlayerHandle.Length; i++)
            writer.Write(nApplyPlayerHandle[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_EFFECT_REQ
{
    public ushort nFurnitureHnd;
    public ushort nEffectHandle;

    public void Read(BinaryReader reader)
    {
        nFurnitureHnd = reader.ReadUInt16();
        nEffectHandle = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nFurnitureHnd);
        writer.Write(nEffectHandle);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_EMOTION_ACK
{
    public ushort nFurnitureHnd;
    public byte nPosition;
    public byte nEmotionNo;
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nFurnitureHnd = reader.ReadUInt16();
        nPosition = reader.ReadByte();
        nEmotionNo = reader.ReadByte();
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nFurnitureHnd);
        writer.Write(nPosition);
        writer.Write(nEmotionNo);
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_EMOTION_CANCEL_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_EMOTION_CANCEL_CMD
{
    public ushort nCancelEmotionCnt;
    public ushort[] nCancelEmotion = [];

    public void Read(BinaryReader reader)
    {
        nCancelEmotionCnt = reader.ReadUInt16();
        nCancelEmotion = new ushort[nCancelEmotionCnt];
        for (int i = 0; i < nCancelEmotionCnt; i++)
            nCancelEmotion[i] = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nCancelEmotionCnt);
        for (int i = 0; i < nCancelEmotion.Length; i++)
            writer.Write(nCancelEmotion[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_EMOTION_CANCEL_REQ
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_EMOTION_CMD
{
    public ushort nEmotionCnt;
    public FURNITURE_EMOTION_INFO[] Emotion = [];

    public void Read(BinaryReader reader)
    {
        nEmotionCnt = reader.ReadUInt16();
        Emotion = new FURNITURE_EMOTION_INFO[nEmotionCnt];
        for (int i = 0; i < nEmotionCnt; i++)
        {
            Emotion[i] = new FURNITURE_EMOTION_INFO();
            Emotion[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nEmotionCnt);
        for (int i = 0; i < Emotion.Length; i++)
            Emotion[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_EMOTION_REQ
{
    public ushort nFurnitureHnd;
    public byte nPosition;
    public byte nEmotionNo;

    public void Read(BinaryReader reader)
    {
        nFurnitureHnd = reader.ReadUInt16();
        nPosition = reader.ReadByte();
        nEmotionNo = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nFurnitureHnd);
        writer.Write(nPosition);
        writer.Write(nEmotionNo);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_FURNITURE_ENDURE_CMD
{
    public byte nEndureFurnitureCnt;
    public ENDURE_FURNITURE_INFO[] EndureFurniture = [];

    public void Read(BinaryReader reader)
    {
        nEndureFurnitureCnt = reader.ReadByte();
        EndureFurniture = new ENDURE_FURNITURE_INFO[nEndureFurnitureCnt];
        for (int i = 0; i < nEndureFurnitureCnt; i++)
        {
            EndureFurniture[i] = new ENDURE_FURNITURE_INFO();
            EndureFurniture[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nEndureFurnitureCnt);
        for (int i = 0; i < EndureFurniture.Length; i++)
            EndureFurniture[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_KICKOUT_CMD
{
    public byte kick;

    public void Read(BinaryReader reader)
    {
        kick = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(kick);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_KICKOUT_REQ
{
    public ushort hnd;

    public void Read(BinaryReader reader)
    {
        hnd = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(hnd);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 140</summary>
public class PROTO_NC_MINIHOUSE_LOGINCOMPLETE_ACK
{
    public int error;
    public byte maxvisiter;
    public byte bItemInfoClose;
    public sbyte[] sNotify = new sbyte[101];
    public sbyte[] TargetKey = new sbyte[32];
    public byte playernum;
    // PROTO_NC_MINIHOUSE_LOGINCOMPLETE_ACK::<unnamed-type-player>[0] player at offset 140 (unsupported type)

    public void Read(BinaryReader reader)
    {
        error = reader.ReadInt32();
        maxvisiter = reader.ReadByte();
        bItemInfoClose = reader.ReadByte();
        for (int i = 0; i < 101; i++)
            sNotify[i] = reader.ReadSByte();
        for (int i = 0; i < 32; i++)
            TargetKey[i] = reader.ReadSByte();
        playernum = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(error);
        writer.Write(maxvisiter);
        writer.Write(bItemInfoClose);
        for (int i = 0; i < 101; i++)
            writer.Write(sNotify[i]);
        for (int i = 0; i < 32; i++)
            writer.Write(TargetKey[i]);
        writer.Write(playernum);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 3</summary>
public class PROTO_NC_MINIHOUSE_LOGINCOMPLETE_CMD
{
    public byte level;
    public ushort hnd;

    public void Read(BinaryReader reader)
    {
        level = reader.ReadByte();
        hnd = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(level);
        writer.Write(hnd);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_LOGOUTCOMPLETE_CMD
{
    public ushort hnd;

    public void Read(BinaryReader reader)
    {
        hnd = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(hnd);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_MODIFY_ITEM_INFO_OPEN_CMD
{
    public byte bItemInfoClose;

    public void Read(BinaryReader reader)
    {
        bItemInfoClose = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bItemInfoClose);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_MODIFY_ITEM_INFO_OPEN_REQ
{
    public byte bItemInfoClose;

    public void Read(BinaryReader reader)
    {
        bItemInfoClose = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bItemInfoClose);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_MODIFY_MAXENTERNUM_REQ
{
    public byte maxvisiter;

    public void Read(BinaryReader reader)
    {
        maxvisiter = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(maxvisiter);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_MODIFY_NOTIFY_CMD
{
    public byte nNotifyLen;
    public sbyte[] sNotify = [];

    public void Read(BinaryReader reader)
    {
        nNotifyLen = reader.ReadByte();
        sNotify = new sbyte[nNotifyLen];
        for (int i = 0; i < nNotifyLen; i++)
            sNotify[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nNotifyLen);
        for (int i = 0; i < sNotify.Length; i++)
            writer.Write(sNotify[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_MODIFY_NOTIFY_REQ
{
    public byte nNotifyLen;
    public sbyte[] sNotify = [];

    public void Read(BinaryReader reader)
    {
        nNotifyLen = reader.ReadByte();
        sNotify = new sbyte[nNotifyLen];
        for (int i = 0; i < nNotifyLen; i++)
            sNotify[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nNotifyLen);
        for (int i = 0; i < sNotify.Length; i++)
            writer.Write(sNotify[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 23</summary>
public class PROTO_NC_MINIHOUSE_MODIFY_OUTSIDE_TITLE_CMD
{
    public ushort mh_hnd;
    public sbyte[] title = new sbyte[21];

    public void Read(BinaryReader reader)
    {
        mh_hnd = reader.ReadUInt16();
        for (int i = 0; i < 21; i++)
            title[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(mh_hnd);
        for (int i = 0; i < 21; i++)
            writer.Write(title[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_MODIFY_PASSWORD_CMD
{
    public byte isPublic;

    public void Read(BinaryReader reader)
    {
        isPublic = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(isPublic);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 9</summary>
public class PROTO_NC_MINIHOUSE_MODIFY_PASSWORD_REQ
{
    public sbyte[] password = new sbyte[9];

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 9; i++)
            password[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 9; i++)
            writer.Write(password[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 21</summary>
public class PROTO_NC_MINIHOUSE_MODIFY_TITLE_REQ
{
    public sbyte[] title = new sbyte[21];

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 21; i++)
            title[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 21; i++)
            writer.Write(title[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_MINIHOUSE_PORTAL_ADD_CMD
{
    public PORTAL_INFO NewPortal = new();

    public void Read(BinaryReader reader)
    {
        NewPortal.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        NewPortal.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_PORTAL_CLOSE_REQ
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_PORTAL_DEL_CMD
{
    public ushort nPortalItemID;

    public void Read(BinaryReader reader)
    {
        nPortalItemID = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPortalItemID);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_PORTAL_EFFECT_ACK
{
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(Error);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MINIHOUSE_PORTAL_EFFECT_CMD
{
    public ushort nEffectHandle;
    public ushort nApplyPlayerCnt;
    public ushort[] nApplyPlayerHandle = [];

    public void Read(BinaryReader reader)
    {
        nEffectHandle = reader.ReadUInt16();
        nApplyPlayerCnt = reader.ReadUInt16();
        nApplyPlayerHandle = new ushort[nApplyPlayerCnt];
        for (int i = 0; i < nApplyPlayerCnt; i++)
            nApplyPlayerHandle[i] = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nEffectHandle);
        writer.Write(nApplyPlayerCnt);
        for (int i = 0; i < nApplyPlayerHandle.Length; i++)
            writer.Write(nApplyPlayerHandle[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_PORTAL_EFFECT_REQ
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_PORTAL_LIST_CMD
{
    public ushort nPortalCnt;
    public PORTAL_INFO[] PortalInfo = [];

    public void Read(BinaryReader reader)
    {
        nPortalCnt = reader.ReadUInt16();
        PortalInfo = new PORTAL_INFO[nPortalCnt];
        for (int i = 0; i < nPortalCnt; i++)
        {
            PortalInfo[i] = new PORTAL_INFO();
            PortalInfo[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPortalCnt);
        for (int i = 0; i < PortalInfo.Length; i++)
            PortalInfo[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_PORTAL_OPEN_REQ
{
    public ushort nPortalItemID;

    public void Read(BinaryReader reader)
    {
        nPortalItemID = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPortalItemID);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_MINIHOUSE_REARRANGE_REQ
{
    public byte furniturecount;
    // PROTO_NC_MINIHOUSE_REARRANGE_REQ::<unnamed-type-rearrange>[0] rearrange at offset 1 (unsupported type)

    public void Read(BinaryReader reader)
    {
        furniturecount = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(furniturecount);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 11</summary>
public class PROTO_NC_MINIHOUSE_VISIT_REQ
{
    public ushort mh_hnd;
    public sbyte[] password = new sbyte[9];

    public void Read(BinaryReader reader)
    {
        mh_hnd = reader.ReadUInt16();
        for (int i = 0; i < 9; i++)
            password[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(mh_hnd);
        for (int i = 0; i < 9; i++)
            writer.Write(password[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 7</summary>
public class PROTO_NC_MINIHOUSE_VISITREADY_ACK
{
    public int error;
    public byte isPublic;
    public byte visitercnt;
    public byte maxvisiter;

    public void Read(BinaryReader reader)
    {
        error = reader.ReadInt32();
        isPublic = reader.ReadByte();
        visitercnt = reader.ReadByte();
        maxvisiter = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(error);
        writer.Write(isPublic);
        writer.Write(visitercnt);
        writer.Write(maxvisiter);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MINIHOUSE_VISITREADY_REQ
{
    public ushort mh_hnd;

    public void Read(BinaryReader reader)
    {
        mh_hnd = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(mh_hnd);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

