// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MOVER_HUNGRY_CMD
{
    public ushort nHungry;

    public void Read(BinaryReader reader)
    {
        nHungry = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nHungry);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_MOVER_MOVESPEED_CMD
{
    public ushort nMoverHandle;
    public ushort nWalk;
    public ushort nRun;

    public void Read(BinaryReader reader)
    {
        nMoverHandle = reader.ReadUInt16();
        nWalk = reader.ReadUInt16();
        nRun = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nMoverHandle);
        writer.Write(nWalk);
        writer.Write(nRun);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MOVER_RIDE_ON_CMD
{
    public ushort nMoverHandle;
    public byte nSlot;
    public byte nGrade;

    public void Read(BinaryReader reader)
    {
        nMoverHandle = reader.ReadUInt16();
        nSlot = reader.ReadByte();
        nGrade = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nMoverHandle);
        writer.Write(nSlot);
        writer.Write(nGrade);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 3</summary>
public class PROTO_NC_MOVER_RIDE_ON_REQ
{
    public ushort nMoverHandle;
    public byte nSlot;

    public void Read(BinaryReader reader)
    {
        nMoverHandle = reader.ReadUInt16();
        nSlot = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nMoverHandle);
        writer.Write(nSlot);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_MOVER_SKILLBASH_FLD_CAST_REQ
{
    public ushort nSkillID;
    public SHINE_XY_TYPE nTargetLoc = new();

    public void Read(BinaryReader reader)
    {
        nSkillID = reader.ReadUInt16();
        nTargetLoc.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nSkillID);
        nTargetLoc.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_MOVER_SKILLBASH_OBJ_CAST_REQ
{
    public ushort nSkillID;
    public ushort nTarget;

    public void Read(BinaryReader reader)
    {
        nSkillID = reader.ReadUInt16();
        nTarget = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nSkillID);
        writer.Write(nTarget);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_MOVER_SOMEONE_RIDE_OFF_CMD
{
    public ushort nHandle;

    public void Read(BinaryReader reader)
    {
        nHandle = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nHandle);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_MOVER_SOMEONE_RIDE_ON_CMD
{
    public ushort nHandle;
    public ushort nMoverHandle;
    public byte nSlot;
    public byte nGrade;

    public void Read(BinaryReader reader)
    {
        nHandle = reader.ReadUInt16();
        nMoverHandle = reader.ReadUInt16();
        nSlot = reader.ReadByte();
        nGrade = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nHandle);
        writer.Write(nMoverHandle);
        writer.Write(nSlot);
        writer.Write(nGrade);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

