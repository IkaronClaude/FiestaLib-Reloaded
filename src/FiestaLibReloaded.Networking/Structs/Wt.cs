// <auto-generated />
using FiestaLibReloaded.Networking;
using FiestaLibReloaded.Networking.Enums;

namespace FiestaLibReloaded.Networking.Structs;

/// <summary>
/// Department: Wt (0x17) | Opcode: 3 | Full: 0x1703
/// sizeof = 3
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 3)]
public class PROTO_NC_WT_GRADE_CMD : IFiestaPacketBody
{
    public ITEM_INVEN ItemInven = new();
    public byte Grade;

    public void Read(BinaryReader reader)
    {
        ItemInven.Read(reader);
        Grade = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        ItemInven.Write(writer);
        writer.Write(Grade);
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 2 | Full: 0x1702
/// sizeof = 7
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 2)]
public class PROTO_NC_WT_LICENSE_ACK : IFiestaPacketBody
{
    public ITEM_INVEN licenseitem = new();
    public ITEM_INVEN weaponitem = new();
    public byte bSet;
    public ushort err;

    public void Read(BinaryReader reader)
    {
        licenseitem.Read(reader);
        weaponitem.Read(reader);
        bSet = reader.ReadByte();
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        licenseitem.Write(writer);
        weaponitem.Write(writer);
        writer.Write(bSet);
        writer.Write(err);
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 8 | Full: 0x1708
/// sizeof = 24
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 8)]
public class PROTO_NC_WT_LICENSE_CLR_ACK : IFiestaPacketBody
{
    public NETPACKETZONEHEADER Header = new();
    public ushort lockindex;
    // PROTO_NC_WT_LICENSE_CLR_ACK::echo_data echo_data at offset 8 (unsupported type)
    public SHINE_ITEM_REGISTNUMBER WeaponItemKey = new();
    public ushort MobID;
    public ushort ErrorCode;

    public void Read(BinaryReader reader)
    {
        Header.Read(reader);
        lockindex = reader.ReadUInt16();
        reader.ReadBytes(4); // skip PROTO_NC_WT_LICENSE_CLR_ACK::echo_data echo_data
        WeaponItemKey.Read(reader);
        MobID = reader.ReadUInt16();
        ErrorCode = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        Header.Write(writer);
        writer.Write(lockindex);
        writer.Write(new byte[4]); // skip PROTO_NC_WT_LICENSE_CLR_ACK::echo_data echo_data
        WeaponItemKey.Write(writer);
        writer.Write(MobID);
        writer.Write(ErrorCode);
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 7 | Full: 0x1707
/// sizeof = 35
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 7)]
public class PROTO_NC_WT_LICENSE_CLR_REQ : IFiestaPacketBody
{
    public NETPACKETZONEHEADER Header = new();
    public ushort lockindex;
    // PROTO_NC_WT_LICENSE_CLR_REQ::echo_data echo_data at offset 8 (unsupported type)
    public SHINE_ITEM_REGISTNUMBER WeaponItemKey = new();
    public ushort MobID;
    public SHINE_ITEM_REGISTNUMBER LicenseClearItemKey = new();
    public ushort LicenseClearItemID;
    public byte LicenseClearItemDelete;
    public ushort LicenseClearAttr;

    public void Read(BinaryReader reader)
    {
        Header.Read(reader);
        lockindex = reader.ReadUInt16();
        reader.ReadBytes(4); // skip PROTO_NC_WT_LICENSE_CLR_REQ::echo_data echo_data
        WeaponItemKey.Read(reader);
        MobID = reader.ReadUInt16();
        LicenseClearItemKey.Read(reader);
        LicenseClearItemID = reader.ReadUInt16();
        LicenseClearItemDelete = reader.ReadByte();
        LicenseClearAttr = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        Header.Write(writer);
        writer.Write(lockindex);
        writer.Write(new byte[4]); // skip PROTO_NC_WT_LICENSE_CLR_REQ::echo_data echo_data
        WeaponItemKey.Write(writer);
        writer.Write(MobID);
        LicenseClearItemKey.Write(writer);
        writer.Write(LicenseClearItemID);
        writer.Write(LicenseClearItemDelete);
        writer.Write(LicenseClearAttr);
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 1 | Full: 0x1701
/// sizeof = 7
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 1)]
public class PROTO_NC_WT_LICENSE_REQ : IFiestaPacketBody
{
    public ITEM_INVEN licenseitem = new();
    public ITEM_INVEN weaponitem = new();
    public ushort mobid;
    public byte bSet;

    public void Read(BinaryReader reader)
    {
        licenseitem.Read(reader);
        weaponitem.Read(reader);
        mobid = reader.ReadUInt16();
        bSet = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        licenseitem.Write(writer);
        weaponitem.Write(writer);
        writer.Write(mobid);
        writer.Write(bSet);
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 6 | Full: 0x1706
/// sizeof = 14
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 6)]
public class PROTO_NC_WT_LICENSE_SET_ACK : IFiestaPacketBody
{
    public NETPACKETZONEHEADER Header = new();
    public ushort Error;
    public ushort lockindex;
    // PROTO_NC_WT_LICENSE_SET_ACK::echo_data echo_data at offset 10 (unsupported type)

    public void Read(BinaryReader reader)
    {
        Header.Read(reader);
        Error = reader.ReadUInt16();
        lockindex = reader.ReadUInt16();
        reader.ReadBytes(4); // skip PROTO_NC_WT_LICENSE_SET_ACK::echo_data echo_data
    }

    public void Write(BinaryWriter writer)
    {
        Header.Write(writer);
        writer.Write(Error);
        writer.Write(lockindex);
        writer.Write(new byte[4]); // skip PROTO_NC_WT_LICENSE_SET_ACK::echo_data echo_data
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 5 | Full: 0x1705
/// sizeof = 54
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 5)]
public class PROTO_NC_WT_LICENSE_SET_REQ : IFiestaPacketBody
{
    public NETPACKETZONEHEADER Header = new();
    public ushort lockindex;
    public uint chrregnum;
    public Name5 chrname = new();
    public SHINE_ITEM_REGISTNUMBER WeaponItemKey = new();
    public SHINE_ITEM_REGISTNUMBER LicenItemKey = new();
    public ushort LicenseMobID;
    // PROTO_NC_WT_LICENSE_SET_REQ::echo_data echo_data at offset 50 (unsupported type)

    public void Read(BinaryReader reader)
    {
        Header.Read(reader);
        lockindex = reader.ReadUInt16();
        chrregnum = reader.ReadUInt32();
        chrname.Read(reader);
        WeaponItemKey.Read(reader);
        LicenItemKey.Read(reader);
        LicenseMobID = reader.ReadUInt16();
        reader.ReadBytes(4); // skip PROTO_NC_WT_LICENSE_SET_REQ::echo_data echo_data
    }

    public void Write(BinaryWriter writer)
    {
        Header.Write(writer);
        writer.Write(lockindex);
        writer.Write(chrregnum);
        chrname.Write(writer);
        WeaponItemKey.Write(writer);
        LicenItemKey.Write(writer);
        writer.Write(LicenseMobID);
        writer.Write(new byte[4]); // skip PROTO_NC_WT_LICENSE_SET_REQ::echo_data echo_data
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 10 | Full: 0x170A
/// sizeof = 22
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 10)]
public class PROTO_NC_WT_MOB_KILLCOUNT_SET_ACK : IFiestaPacketBody
{
    public NETPACKETZONEHEADER Header = new();
    public SHINE_ITEM_REGISTNUMBER ItemKey = new();
    public ushort MobID;
    public uint MobKillCount;
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        Header.Read(reader);
        ItemKey.Read(reader);
        MobID = reader.ReadUInt16();
        MobKillCount = reader.ReadUInt32();
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        Header.Write(writer);
        ItemKey.Write(writer);
        writer.Write(MobID);
        writer.Write(MobKillCount);
        writer.Write(Error);
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 9 | Full: 0x1709
/// sizeof = 20
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 9)]
public class PROTO_NC_WT_MOB_KILLCOUNT_SET_REQ : IFiestaPacketBody
{
    public NETPACKETZONEHEADER Header = new();
    public SHINE_ITEM_REGISTNUMBER ItemKey = new();
    public ushort MobID;
    public uint MobKillCount;

    public void Read(BinaryReader reader)
    {
        Header.Read(reader);
        ItemKey.Read(reader);
        MobID = reader.ReadUInt16();
        MobKillCount = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        Header.Write(writer);
        ItemKey.Write(writer);
        writer.Write(MobID);
        writer.Write(MobKillCount);
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 13 | Full: 0x170D
/// sizeof = 2
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 13)]
public class PROTO_NC_WT_MOBINC_CMD : IFiestaPacketBody
{
    public ushort mob;

    public void Read(BinaryReader reader)
    {
        mob = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(mob);
    }
}

/// <summary>
/// Department: Wt (0x17) | Opcode: 12 | Full: 0x170C
/// sizeof = 8
/// </summary>
[FiestaOpcode(ProtocolCommand.Wt, 12)]
public class PROTO_NC_WT_TITLE_SET_ACK : IFiestaPacketBody
{
    public NETPACKETZONEHEADER Header = new();
    public ushort Error;

    public void Read(BinaryReader reader)
    {
        Header.Read(reader);
        Error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        Header.Write(writer);
        writer.Write(Error);
    }
}

