// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 6</summary>
public class PROTO_NC_ZONE_PRISON_END_CMD
{
    public ushort nCharZoneHandle;
    public uint nCharRegNum;

    public void Read(BinaryReader reader)
    {
        nCharZoneHandle = reader.ReadUInt16();
        nCharRegNum = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nCharZoneHandle);
        writer.Write(nCharRegNum);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 374</summary>
public class PROTO_NC_ZONE_PRISON_GO_ACK
{
    public ushort nGmHandler;
    public uint nGmRegNum;
    public Name256Byte sGmUserID = new();
    public Name5 sCharID = new();
    public ushort nCharSessHandler;
    public ushort nCharZoneHandle;
    public uint nCharRegNum;
    public ushort nMinute;
    public sbyte[] sReason = new sbyte[16];
    public sbyte[] sRemark = new sbyte[64];
    public ushort err;

    public void Read(BinaryReader reader)
    {
        nGmHandler = reader.ReadUInt16();
        nGmRegNum = reader.ReadUInt32();
        sGmUserID.Read(reader);
        sCharID.Read(reader);
        nCharSessHandler = reader.ReadUInt16();
        nCharZoneHandle = reader.ReadUInt16();
        nCharRegNum = reader.ReadUInt32();
        nMinute = reader.ReadUInt16();
        for (int i = 0; i < 16; i++)
            sReason[i] = reader.ReadSByte();
        for (int i = 0; i < 64; i++)
            sRemark[i] = reader.ReadSByte();
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nGmHandler);
        writer.Write(nGmRegNum);
        sGmUserID.Write(writer);
        sCharID.Write(writer);
        writer.Write(nCharSessHandler);
        writer.Write(nCharZoneHandle);
        writer.Write(nCharRegNum);
        writer.Write(nMinute);
        for (int i = 0; i < 16; i++)
            writer.Write(sReason[i]);
        for (int i = 0; i < 64; i++)
            writer.Write(sRemark[i]);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 372</summary>
public class PROTO_NC_ZONE_PRISON_GO_REQ
{
    public ushort nGmHandler;
    public uint nGmRegNum;
    public Name256Byte sGmUserID = new();
    public Name5 sCharID = new();
    public ushort nCharSessHandle;
    public ushort nCharZoneHandle;
    public uint nCharRegNum;
    public ushort nMinute;
    public sbyte[] sReason = new sbyte[16];
    public sbyte[] sRemark = new sbyte[64];

    public void Read(BinaryReader reader)
    {
        nGmHandler = reader.ReadUInt16();
        nGmRegNum = reader.ReadUInt32();
        sGmUserID.Read(reader);
        sCharID.Read(reader);
        nCharSessHandle = reader.ReadUInt16();
        nCharZoneHandle = reader.ReadUInt16();
        nCharRegNum = reader.ReadUInt32();
        nMinute = reader.ReadUInt16();
        for (int i = 0; i < 16; i++)
            sReason[i] = reader.ReadSByte();
        for (int i = 0; i < 64; i++)
            sRemark[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nGmHandler);
        writer.Write(nGmRegNum);
        sGmUserID.Write(writer);
        sCharID.Write(writer);
        writer.Write(nCharSessHandle);
        writer.Write(nCharZoneHandle);
        writer.Write(nCharRegNum);
        writer.Write(nMinute);
        for (int i = 0; i < 16; i++)
            writer.Write(sReason[i]);
        for (int i = 0; i < 64; i++)
            writer.Write(sRemark[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

