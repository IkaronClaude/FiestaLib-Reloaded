// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 7</summary>
public class PROTO_NC_DICE_TAISAI_BET_START_CMD
{
    // DICE_TAISAI_GAME_MODE bModeType at offset 0 (unsupported type)
    public DiceTaiSaiInfo CurrentRollingDice = new();

    public void Read(BinaryReader reader)
    {
        CurrentRollingDice.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        CurrentRollingDice.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_DICE_TAISAI_BETTING_ACK
{
    public ushort nError;
    public ushort nBettingIndex;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nBettingIndex = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nBettingIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_DICE_TAISAI_BETTING_CANCEL_ACK
{
    public ushort nError;
    public ushort nBettingCancelIndex;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nBettingCancelIndex = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nBettingCancelIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_DICE_TAISAI_BETTING_CANCEL_CMD
{
    public ushort nPlayerHnd;
    public DiceTaiSaiBetting BettingCancelInfo = new();

    public void Read(BinaryReader reader)
    {
        nPlayerHnd = reader.ReadUInt16();
        BettingCancelInfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPlayerHnd);
        BettingCancelInfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_DICE_TAISAI_BETTING_CANCEL_REQ
{
    public DiceTaiSaiBetting BettingCancelInfo = new();
    public ushort nBettingCancelIndex;

    public void Read(BinaryReader reader)
    {
        BettingCancelInfo.Read(reader);
        nBettingCancelIndex = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        BettingCancelInfo.Write(writer);
        writer.Write(nBettingCancelIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_DICE_TAISAI_BETTING_CMD
{
    public ushort nPlayerHnd;
    public DiceTaiSaiBetting BettingInfo = new();

    public void Read(BinaryReader reader)
    {
        nPlayerHnd = reader.ReadUInt16();
        BettingInfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPlayerHnd);
        BettingInfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 10</summary>
public class PROTO_NC_DICE_TAISAI_BETTING_REQ
{
    public DiceTaiSaiBetting BettingInfo = new();
    public ushort nBettingIndex;

    public void Read(BinaryReader reader)
    {
        BettingInfo.Read(reader);
        nBettingIndex = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        BettingInfo.Write(writer);
        writer.Write(nBettingIndex);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_DICE_TAISAI_BOARD_FOLD_CMD
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_DICE_TAISAI_DB_DICE_ROLL_RESULT_ACK
{
    public ushort nError;
    public int nGameNo;
    public NETPACKETZONEHEADER header = new();
    public SHINE_ITEM_REGISTNUMBER nActiveDiceTaiSaiKey = new();
    public ushort nResultCnt;
    public DiceRollApplyResult[] DiceRollResult = [];

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nGameNo = reader.ReadInt32();
        header.Read(reader);
        nActiveDiceTaiSaiKey.Read(reader);
        nResultCnt = reader.ReadUInt16();
        DiceRollResult = new DiceRollApplyResult[nResultCnt];
        for (int i = 0; i < nResultCnt; i++)
        {
            DiceRollResult[i] = new DiceRollApplyResult();
            DiceRollResult[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nGameNo);
        header.Write(writer);
        nActiveDiceTaiSaiKey.Write(writer);
        writer.Write(nResultCnt);
        for (int i = 0; i < DiceRollResult.Length; i++)
            DiceRollResult[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 20</summary>
public class PROTO_NC_DICE_TAISAI_DB_DICE_ROLL_RESULT_LOG_CMD
{
    public NETPACKETZONEHEADER header = new();
    public SHINE_ITEM_REGISTNUMBER nActiveDiceTaiSaiKey = new();
    public int nGameNo;
    public ushort nResultLogCnt;
    public DiceRollApplyResultLog[] DiceRollResultLog = [];

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        nActiveDiceTaiSaiKey.Read(reader);
        nGameNo = reader.ReadInt32();
        nResultLogCnt = reader.ReadUInt16();
        DiceRollResultLog = new DiceRollApplyResultLog[nResultLogCnt];
        for (int i = 0; i < nResultLogCnt; i++)
        {
            DiceRollResultLog[i] = new DiceRollApplyResultLog();
            DiceRollResultLog[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        nActiveDiceTaiSaiKey.Write(writer);
        writer.Write(nGameNo);
        writer.Write(nResultLogCnt);
        for (int i = 0; i < DiceRollResultLog.Length; i++)
            DiceRollResultLog[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 19</summary>
public class PROTO_NC_DICE_TAISAI_DB_DICE_ROLL_RESULT_REQ
{
    public NETPACKETZONEHEADER header = new();
    public SHINE_ITEM_REGISTNUMBER nActiveDiceTaiSaiKey = new();
    public DiceTaiSaiInfo DiceNum = new();
    public ushort nResultCnt;
    public DiceRollApplyResult[] DiceRollResult = [];

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        nActiveDiceTaiSaiKey.Read(reader);
        DiceNum.Read(reader);
        nResultCnt = reader.ReadUInt16();
        DiceRollResult = new DiceRollApplyResult[nResultCnt];
        for (int i = 0; i < nResultCnt; i++)
        {
            DiceRollResult[i] = new DiceRollApplyResult();
            DiceRollResult[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        nActiveDiceTaiSaiKey.Write(writer);
        DiceNum.Write(writer);
        writer.Write(nResultCnt);
        for (int i = 0; i < DiceRollResult.Length; i++)
            DiceRollResult[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 30</summary>
public class PROTO_NC_DICE_TAISAI_DB_EXPECT_INCOME_MONEY_ACK
{
    public ushort nError;
    public NETPACKETZONEHEADER header = new();
    public ITEM_INVEN nInvenSlot = new();
    public SHINE_ITEM_REGISTNUMBER nTaiSaiItemKey = new();
    public uint nGameCnt;
    public long nExpectIncomeMoney;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        header.Read(reader);
        nInvenSlot.Read(reader);
        nTaiSaiItemKey.Read(reader);
        nGameCnt = reader.ReadUInt32();
        nExpectIncomeMoney = reader.ReadInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        header.Write(writer);
        nInvenSlot.Write(writer);
        nTaiSaiItemKey.Write(writer);
        writer.Write(nGameCnt);
        writer.Write(nExpectIncomeMoney);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 26</summary>
public class PROTO_NC_DICE_TAISAI_DB_EXPECT_INCOME_MONEY_REQ
{
    public NETPACKETZONEHEADER header = new();
    public ITEM_INVEN nInvenSlot = new();
    public SHINE_ITEM_REGISTNUMBER nTaiSaiItemKey = new();
    public ulong nRewardMoney;
    public ushort GetMasterRate;

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        nInvenSlot.Read(reader);
        nTaiSaiItemKey.Read(reader);
        nRewardMoney = reader.ReadUInt64();
        GetMasterRate = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        nInvenSlot.Write(writer);
        nTaiSaiItemKey.Write(writer);
        writer.Write(nRewardMoney);
        writer.Write(GetMasterRate);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 32</summary>
public class PROTO_NC_DICE_TAISAI_DB_ITEM_ID_CHANGE_ACK
{
    public ushort nError;
    public NETPACKETZONEHEADER header = new();
    public ITEM_INVEN nInvenSlot = new();
    public ushort lockindex;
    public SHINE_ITEM_REGISTNUMBER nDiceTaiSaiKey = new();
    public ushort nGameItemID;
    public ushort nMoneyCardItemID;
    public ulong nRewardMoney;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        header.Read(reader);
        nInvenSlot.Read(reader);
        lockindex = reader.ReadUInt16();
        nDiceTaiSaiKey.Read(reader);
        nGameItemID = reader.ReadUInt16();
        nMoneyCardItemID = reader.ReadUInt16();
        nRewardMoney = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        header.Write(writer);
        nInvenSlot.Write(writer);
        writer.Write(lockindex);
        nDiceTaiSaiKey.Write(writer);
        writer.Write(nGameItemID);
        writer.Write(nMoneyCardItemID);
        writer.Write(nRewardMoney);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 32</summary>
public class PROTO_NC_DICE_TAISAI_DB_ITEM_ID_CHANGE_REQ
{
    public NETPACKETZONEHEADER header = new();
    public ITEM_INVEN nInvenSlot = new();
    public ushort lockindex;
    public SHINE_ITEM_REGISTNUMBER nDiceTaiSaiKey = new();
    public ushort nGameItemID;
    public ushort nMoneyCardItemID;
    public ulong nRewardMoney;
    public ushort GetMasterRate;

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        nInvenSlot.Read(reader);
        lockindex = reader.ReadUInt16();
        nDiceTaiSaiKey.Read(reader);
        nGameItemID = reader.ReadUInt16();
        nMoneyCardItemID = reader.ReadUInt16();
        nRewardMoney = reader.ReadUInt64();
        GetMasterRate = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        nInvenSlot.Write(writer);
        writer.Write(lockindex);
        nDiceTaiSaiKey.Write(writer);
        writer.Write(nGameItemID);
        writer.Write(nMoneyCardItemID);
        writer.Write(nRewardMoney);
        writer.Write(GetMasterRate);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 13</summary>
public class PROTO_NC_DICE_TAISAI_DB_RANK_ACK
{
    public ushort nError;
    public NETPACKETZONEHEADER header = new();
    public uint nReqCharNo;
    public byte nRankCnt;
    public DiceRankInfo[] Rank = [];

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        header.Read(reader);
        nReqCharNo = reader.ReadUInt32();
        nRankCnt = reader.ReadByte();
        Rank = new DiceRankInfo[nRankCnt];
        for (int i = 0; i < nRankCnt; i++)
        {
            Rank[i] = new DiceRankInfo();
            Rank[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        header.Write(writer);
        writer.Write(nReqCharNo);
        writer.Write(nRankCnt);
        for (int i = 0; i < Rank.Length; i++)
            Rank[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 18</summary>
public class PROTO_NC_DICE_TAISAI_DB_RANK_REQ
{
    public NETPACKETZONEHEADER header = new();
    public uint nReqCharNo;
    public SHINE_ITEM_REGISTNUMBER nTaiSaiItemKey = new();

    public void Read(BinaryReader reader)
    {
        header.Read(reader);
        nReqCharNo = reader.ReadUInt32();
        nTaiSaiItemKey.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        header.Write(writer);
        writer.Write(nReqCharNo);
        nTaiSaiItemKey.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 28</summary>
public class PROTO_NC_DICE_TAISAI_DB_RECEIPT_INCOME_MONEY_ACK
{
    public ushort nError;
    public ushort lockindex;
    public NETPACKETZONEHEADER header = new();
    public ITEM_INVEN nInvenSlot = new();
    public SHINE_ITEM_REGISTNUMBER nTaiSaiItemKey = new();
    public ulong nReceiptIncomeMoney;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        lockindex = reader.ReadUInt16();
        header.Read(reader);
        nInvenSlot.Read(reader);
        nTaiSaiItemKey.Read(reader);
        nReceiptIncomeMoney = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(lockindex);
        header.Write(writer);
        nInvenSlot.Write(writer);
        nTaiSaiItemKey.Write(writer);
        writer.Write(nReceiptIncomeMoney);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 18</summary>
public class PROTO_NC_DICE_TAISAI_DB_RECEIPT_INCOME_MONEY_REQ
{
    public ushort lockindex;
    public NETPACKETZONEHEADER header = new();
    public ITEM_INVEN nInvenSlot = new();
    public SHINE_ITEM_REGISTNUMBER nTaiSaiItemKey = new();

    public void Read(BinaryReader reader)
    {
        lockindex = reader.ReadUInt16();
        header.Read(reader);
        nInvenSlot.Read(reader);
        nTaiSaiItemKey.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(lockindex);
        header.Write(writer);
        nInvenSlot.Write(writer);
        nTaiSaiItemKey.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_DICE_TAISAI_DICE_ROLL_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_DICE_TAISAI_DICE_ROLL_CMD
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 5</summary>
public class PROTO_NC_DICE_TAISAI_DICE_ROLL_RESULT_CMD
{
    public ushort nError;
    public DiceTaiSaiInfo nDiceInfo = new();

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nDiceInfo.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        nDiceInfo.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_DICE_TAISAI_DICE_ROLL_RESULT_EMOTION_CMD
{
    public byte nGamerCnt;
    public EmotionInfo[] Emotion = [];

    public void Read(BinaryReader reader)
    {
        nGamerCnt = reader.ReadByte();
        Emotion = new EmotionInfo[nGamerCnt];
        for (int i = 0; i < nGamerCnt; i++)
        {
            Emotion[i] = new EmotionInfo();
            Emotion[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nGamerCnt);
        for (int i = 0; i < Emotion.Length; i++)
            Emotion[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 16</summary>
public class PROTO_NC_DICE_TAISAI_EXPECT_INCOME_MONEY_ACK
{
    public ushort nError;
    public ITEM_INVEN nInvenSlot = new();
    public uint nGameCnt;
    public ulong nExpectIncomeMoney;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nInvenSlot.Read(reader);
        nGameCnt = reader.ReadUInt32();
        nExpectIncomeMoney = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        nInvenSlot.Write(writer);
        writer.Write(nGameCnt);
        writer.Write(nExpectIncomeMoney);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_DICE_TAISAI_EXPECT_INCOME_MONEY_REQ
{
    public ITEM_INVEN nInvenSlot = new();

    public void Read(BinaryReader reader)
    {
        nInvenSlot.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        nInvenSlot.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 3</summary>
public class PROTO_NC_DICE_TAISAI_GAME_BETTING_INFO_CMD
{
    public byte PartMask;
    public ushort nBettingCnt;
    public BettingInfo[] Betting = [];

    public void Read(BinaryReader reader)
    {
        PartMask = reader.ReadByte();
        nBettingCnt = reader.ReadUInt16();
        Betting = new BettingInfo[nBettingCnt];
        for (int i = 0; i < nBettingCnt; i++)
        {
            Betting[i] = new BettingInfo();
            Betting[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(PartMask);
        writer.Write(nBettingCnt);
        for (int i = 0; i < Betting.Length; i++)
            Betting[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 50</summary>
public class PROTO_NC_DICE_TAISAI_GAME_JOIN_ACK
{
    public ushort nError;
    public DiceTaiSaiInfo[] DiceHistory = new DiceTaiSaiInfo[7];
    // DICE_TAISAI_GAME_STATUS nGameStatus at offset 23 (unsupported type)
    // DICE_TAISAI_GAME_MODE bModeType at offset 27 (unsupported type)
    public ushort nTimer;
    public uint nInterval;
    public ushort nGetSysRate;
    public ulong nMaxBetMoney;
    public ushort nUseMinLv;
    public byte nGamerCnt;
    public ushort[] nGamerHnd = [];

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        for (int i = 0; i < 7; i++)
        {
            DiceHistory[i] = new DiceTaiSaiInfo();
            DiceHistory[i].Read(reader);
        }
        nTimer = reader.ReadUInt16();
        nInterval = reader.ReadUInt32();
        nGetSysRate = reader.ReadUInt16();
        nMaxBetMoney = reader.ReadUInt64();
        nUseMinLv = reader.ReadUInt16();
        nGamerCnt = reader.ReadByte();
        nGamerHnd = new ushort[nGamerCnt];
        for (int i = 0; i < nGamerCnt; i++)
            nGamerHnd[i] = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        for (int i = 0; i < 7; i++)
            DiceHistory[i].Write(writer);
        writer.Write(nTimer);
        writer.Write(nInterval);
        writer.Write(nGetSysRate);
        writer.Write(nMaxBetMoney);
        writer.Write(nUseMinLv);
        writer.Write(nGamerCnt);
        for (int i = 0; i < nGamerHnd.Length; i++)
            writer.Write(nGamerHnd[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_DICE_TAISAI_GAME_JOIN_CMD
{
    public ushort nPlayerHnd;

    public void Read(BinaryReader reader)
    {
        nPlayerHnd = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nPlayerHnd);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_DICE_TAISAI_GAME_LEAVE_ACK
{
    public ushort nError;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 3</summary>
public class PROTO_NC_DICE_TAISAI_GAME_LEAVE_CMD
{
    public byte bReserve;
    public ushort nPlayerHnd;

    public void Read(BinaryReader reader)
    {
        bReserve = reader.ReadByte();
        nPlayerHnd = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bReserve);
        writer.Write(nPlayerHnd);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_DICE_TAISAI_GAME_MODE_CHANGE_ACK
{
    public ushort nError;
    // DICE_TAISAI_GAME_MODE bModeType at offset 2 (unsupported type)

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 4</summary>
public class PROTO_NC_DICE_TAISAI_GAME_MODE_CHANGE_CMD
{
    // DICE_TAISAI_GAME_MODE bModeType at offset 0 (unsupported type)

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_DICE_TAISAI_LARGE_AMOUNT_CMD
{
    public byte nGamerCnt;
    public LargeAmountInfo[] LargeAmount = [];

    public void Read(BinaryReader reader)
    {
        nGamerCnt = reader.ReadByte();
        LargeAmount = new LargeAmountInfo[nGamerCnt];
        for (int i = 0; i < nGamerCnt; i++)
        {
            LargeAmount[i] = new LargeAmountInfo();
            LargeAmount[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nGamerCnt);
        for (int i = 0; i < LargeAmount.Length; i++)
            LargeAmount[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 484</summary>
public class PROTO_NC_DICE_TAISAI_RANK_ACK
{
    public ushort nError;
    public byte nRankHistoryCnt;
    public DiceRankInfo[] RankHistory = new DiceRankInfo[20];
    public byte nRankCurrCnt;
    public DiceRankInfo[] RankCurr = [];

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nRankHistoryCnt = reader.ReadByte();
        for (int i = 0; i < 20; i++)
        {
            RankHistory[i] = new DiceRankInfo();
            RankHistory[i].Read(reader);
        }
        nRankCurrCnt = reader.ReadByte();
        RankCurr = new DiceRankInfo[nRankCurrCnt];
        for (int i = 0; i < nRankCurrCnt; i++)
        {
            RankCurr[i] = new DiceRankInfo();
            RankCurr[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(nRankHistoryCnt);
        for (int i = 0; i < 20; i++)
            RankHistory[i].Write(writer);
        writer.Write(nRankCurrCnt);
        for (int i = 0; i < RankCurr.Length; i++)
            RankCurr[i].Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 12</summary>
public class PROTO_NC_DICE_TAISAI_RECEIPT_INCOME_MONEY_ACK
{
    public ushort nError;
    public ITEM_INVEN nInvenSlot = new();
    public ulong ReceiptIncomeMoney;

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        nInvenSlot.Read(reader);
        ReceiptIncomeMoney = reader.ReadUInt64();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        nInvenSlot.Write(writer);
        writer.Write(ReceiptIncomeMoney);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 2</summary>
public class PROTO_NC_DICE_TAISAI_RECEIPT_INCOME_MONEY_REQ
{
    public ITEM_INVEN nInvenSlot = new();

    public void Read(BinaryReader reader)
    {
        nInvenSlot.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        nInvenSlot.Write(writer);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 7</summary>
public class PROTO_NC_DICE_TAISAI_TIMER_CMD
{
    public byte bIsStart;
    public ushort nTimer;
    public uint nInterval;

    public void Read(BinaryReader reader)
    {
        bIsStart = reader.ReadByte();
        nTimer = reader.ReadUInt16();
        nInterval = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bIsStart);
        writer.Write(nTimer);
        writer.Write(nInterval);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

