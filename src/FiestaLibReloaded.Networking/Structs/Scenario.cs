// <auto-generated />
using FiestaLibReloaded.Networking;
using FiestaLibReloaded.Networking.Enums;

namespace FiestaLibReloaded.Networking.Structs;

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 13 | Full: 0x6C0D
/// sizeof = 34
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 13)]
public class PROTO_NC_SCENARIO_ANIMATE_CMD : IFiestaPacketBody
{
    public ushort handle;
    public Name8 anicode = new();

    public void Read(BinaryReader reader)
    {
        handle = reader.ReadUInt16();
        anicode.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(handle);
        anicode.Write(writer);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 6 | Full: 0x6C06
/// sizeof = 32
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 6)]
public class PROTO_NC_SCENARIO_AREAENTRY_ACK : IFiestaPacketBody
{
    public Name8 areaindex = new();

    public void Read(BinaryReader reader)
    {
        areaindex.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        areaindex.Write(writer);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 5 | Full: 0x6C05
/// sizeof = 32
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 5)]
public class PROTO_NC_SCENARIO_AREAENTRY_REQ : IFiestaPacketBody
{
    public Name8 areaindex = new();

    public void Read(BinaryReader reader)
    {
        areaindex.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        areaindex.Write(writer);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 8 | Full: 0x6C08
/// sizeof = 32
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 8)]
public class PROTO_NC_SCENARIO_AREALEAVE_ACK : IFiestaPacketBody
{
    public Name8 areaindex = new();

    public void Read(BinaryReader reader)
    {
        areaindex.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        areaindex.Write(writer);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 7 | Full: 0x6C07
/// sizeof = 32
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 7)]
public class PROTO_NC_SCENARIO_AREALEAVE_REQ : IFiestaPacketBody
{
    public Name8 areaindex = new();

    public void Read(BinaryReader reader)
    {
        areaindex.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        areaindex.Write(writer);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 16 | Full: 0x6C10
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 16)]
public class PROTO_NC_SCENARIO_BRIEFEFFECT_CMD : IFiestaPacketBody
{
    public byte effectcode;

    public void Read(BinaryReader reader)
    {
        effectcode = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(effectcode);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 20 | Full: 0x6C14
/// sizeof = 15
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 20)]
public class PROTO_NC_SCENARIO_CAMERA_MOVE_CMD : IFiestaPacketBody
{
    public byte MoveType;
    public SHINE_XY_TYPE ViewCoord = new();
    public ushort AngleXZ;
    public ushort AngleY;
    public ushort Dist;

    public void Read(BinaryReader reader)
    {
        MoveType = reader.ReadByte();
        ViewCoord.Read(reader);
        AngleXZ = reader.ReadUInt16();
        AngleY = reader.ReadUInt16();
        Dist = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(MoveType);
        ViewCoord.Write(writer);
        writer.Write(AngleXZ);
        writer.Write(AngleY);
        writer.Write(Dist);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 12 | Full: 0x6C0C
/// sizeof = 3
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 12)]
public class PROTO_NC_SCENARIO_CHATWIN_CMD : IFiestaPacketBody
{
    public ushort mob;
    public byte len;
    public byte[] content = [];

    public void Read(BinaryReader reader)
    {
        mob = reader.ReadUInt16();
        len = reader.ReadByte();
        content = reader.ReadBytes(len);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(mob);
        writer.Write(len);
        writer.Write(content);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 31 | Full: 0x6C1F
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 31)]
public class PROTO_NC_SCENARIO_DEL_DIRECTIONAL_ARROW_CMD : IFiestaPacketBody
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 25 | Full: 0x6C19
/// sizeof = 34
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 25)]
public class PROTO_NC_SCENARIO_DIALOG_CMD : IFiestaPacketBody
{
    public sbyte[] sFacecutIndex = new sbyte[33];
    public byte nLen;
    public byte[] sContents = [];

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 33; i++)
            sFacecutIndex[i] = reader.ReadSByte();
        nLen = reader.ReadByte();
        sContents = reader.ReadBytes(nLen);
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 33; i++)
            writer.Write(sFacecutIndex[i]);
        writer.Write(nLen);
        writer.Write(sContents);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 30 | Full: 0x6C1E
/// sizeof = 8
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 30)]
public class PROTO_NC_SCENARIO_DIRECTIONAL_ARROW_CMD : IFiestaPacketBody
{
    public SHINE_XY_TYPE DirectionalPos = new();

    public void Read(BinaryReader reader)
    {
        DirectionalPos.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        DirectionalPos.Write(writer);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 9 | Full: 0x6C09
/// sizeof = 3
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 9)]
public class PROTO_NC_SCENARIO_DOORSTATE_CMD : IFiestaPacketBody
{
    public ushort door;
    public byte doorstate;

    public void Read(BinaryReader reader)
    {
        door = reader.ReadUInt16();
        doorstate = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(door);
        writer.Write(doorstate);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 29 | Full: 0x6C1D
/// sizeof = 3
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 29)]
public class PROTO_NC_SCENARIO_EFFECT_TIMER_CMD : IFiestaPacketBody
{
    public byte nTimerFunction;
    public ushort nSecond;

    public void Read(BinaryReader reader)
    {
        nTimerFunction = reader.ReadByte();
        nSecond = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nTimerFunction);
        writer.Write(nSecond);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 14 | Full: 0x6C0E
/// sizeof = 3
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 14)]
public class PROTO_NC_SCENARIO_FOG_CMD : IFiestaPacketBody
{
    public byte fog;
    public ushort depth;

    public void Read(BinaryReader reader)
    {
        fog = reader.ReadByte();
        depth = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(fog);
        writer.Write(depth);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 28 | Full: 0x6C1C
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 28)]
public class PROTO_NC_SCENARIO_HIDE_OTHER_PLAYER_CMD : IFiestaPacketBody
{
    public byte bIsHide;

    public void Read(BinaryReader reader)
    {
        bIsHide = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bIsHide);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 10 | Full: 0x6C0A
/// sizeof = 3
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 10)]
public class PROTO_NC_SCENARIO_LIGHT_CMD : IFiestaPacketBody
{
    public byte red;
    public byte green;
    public byte blue;

    public void Read(BinaryReader reader)
    {
        red = reader.ReadByte();
        green = reader.ReadByte();
        blue = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(red);
        writer.Write(green);
        writer.Write(blue);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 24 | Full: 0x6C18
/// sizeof = 37
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 24)]
public class PROTO_NC_SCENARIO_MAP_OBJECT_CONTROL_CMD : IFiestaPacketBody
{
    public sbyte[] sObjectIndex = new sbyte[32];
    public byte IsEnableObject;
    // MAP_OBJECT_CONTROL_TYPE eControlType at offset 33 (unsupported type)

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 32; i++)
            sObjectIndex[i] = reader.ReadSByte();
        IsEnableObject = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 32; i++)
            writer.Write(sObjectIndex[i]);
        writer.Write(IsEnableObject);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 23 | Full: 0x6C17
/// sizeof = 2
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 23)]
public class PROTO_NC_SCENARIO_MAPMARK_CMD : IFiestaPacketBody
{
    public ushort nNum;
    // PROTO_NC_SCENARIO_MAPMARK_CMD::IconInfo[0] IconList at offset 2 (unsupported type)

    public void Read(BinaryReader reader)
    {
        nNum = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nNum);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 18 | Full: 0x6C12
/// sizeof = 17
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 18)]
public class PROTO_NC_SCENARIO_MESSAGE_CMD : IFiestaPacketBody
{
    public sbyte[] DialogIndex = new sbyte[17];

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 17; i++)
            DialogIndex[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 17; i++)
            writer.Write(DialogIndex[i]);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 4 | Full: 0x6C04
/// sizeof = 4
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 4)]
public class PROTO_NC_SCENARIO_MESSAGENOTICE_CMD : IFiestaPacketBody
{
    public uint message;

    public void Read(BinaryReader reader)
    {
        message = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(message);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 17 | Full: 0x6C11
/// sizeof = 35
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 17)]
public class PROTO_NC_SCENARIO_NPCCHAT_CMD : IFiestaPacketBody
{
    public sbyte[] DialogIndex = new sbyte[33];
    public ushort NPCHandle;

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 33; i++)
            DialogIndex[i] = reader.ReadSByte();
        NPCHandle = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 33; i++)
            writer.Write(DialogIndex[i]);
        writer.Write(NPCHandle);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 11 | Full: 0x6C0B
/// sizeof = 3
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 11)]
public class PROTO_NC_SCENARIO_OBJTYPECHANGE_CMD : IFiestaPacketBody
{
    public ushort handle;
    public byte type;

    public void Read(BinaryReader reader)
    {
        handle = reader.ReadUInt16();
        type = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(handle);
        writer.Write(type);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 32 | Full: 0x6C20
/// sizeof = 1000
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 32)]
public class PROTO_NC_SCENARIO_RANKING_LIST_CMD : IFiestaPacketBody
{
    public SCENARIO_RANKING_INFO[] PreviousRanking = new SCENARIO_RANKING_INFO[20];
    public SCENARIO_RANKING_INFO[] CurrentRanking = new SCENARIO_RANKING_INFO[20];

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 20; i++)
        {
            PreviousRanking[i] = new SCENARIO_RANKING_INFO();
            PreviousRanking[i].Read(reader);
        }
        for (int i = 0; i < 20; i++)
        {
            CurrentRanking[i] = new SCENARIO_RANKING_INFO();
            CurrentRanking[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 20; i++)
            PreviousRanking[i].Write(writer);
        for (int i = 0; i < 20; i++)
            CurrentRanking[i].Write(writer);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 21 | Full: 0x6C15
/// sizeof = 37
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 21)]
public class PROTO_NC_SCENARIO_SCRIPTMESSAGE_CMD : IFiestaPacketBody
{
    public sbyte[] DialogIndex = new sbyte[32];
    public byte[] nlen = new byte[5];
    public byte[] sContent = [];

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 32; i++)
            DialogIndex[i] = reader.ReadSByte();
        nlen = reader.ReadBytes(5);
        // TODO: sContent is variable-length but count field not detected
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 32; i++)
            writer.Write(DialogIndex[i]);
        writer.Write(nlen, 0, 5);
        writer.Write(sContent);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 27 | Full: 0x6C1B
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 27)]
public class PROTO_NC_SCENARIO_SYSTEM_MESSAGE_CMD : IFiestaPacketBody
{
    public byte nLen;
    public byte[] sMessage = [];

    public void Read(BinaryReader reader)
    {
        nLen = reader.ReadByte();
        sMessage = reader.ReadBytes(nLen);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nLen);
        writer.Write(sMessage);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 22 | Full: 0x6C16
/// sizeof = 2
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 22)]
public class PROTO_NC_SCENARIO_TIMER_CMD : IFiestaPacketBody
{
    public ushort nSecond;

    public void Read(BinaryReader reader)
    {
        nSecond = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nSecond);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 34 | Full: 0x6C22
/// sizeof = 4
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 34)]
public class PROTO_NC_SCENARIO_TIMER_END_CMD : IFiestaPacketBody
{
    public uint nDeleteSec;

    public void Read(BinaryReader reader)
    {
        nDeleteSec = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nDeleteSec);
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 33 | Full: 0x6C21
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 33)]
public class PROTO_NC_SCENARIO_TIMER_START_CMD : IFiestaPacketBody
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }
}

/// <summary>
/// Department: Scenario (0x1B) | Opcode: 26 | Full: 0x6C1A
/// sizeof = 13
/// </summary>
[FiestaOpcode(ProtocolCommand.Scenario, 26)]
public class PROTO_NC_SCENARIO_TOPVIEW_CMD : IFiestaPacketBody
{
    public byte nMoveType;
    public SHINE_XY_TYPE CenterPos = new();
    public ushort nDistance;
    public ushort nAngle;

    public void Read(BinaryReader reader)
    {
        nMoveType = reader.ReadByte();
        CenterPos.Read(reader);
        nDistance = reader.ReadUInt16();
        nAngle = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nMoveType);
        CenterPos.Write(writer);
        writer.Write(nDistance);
        writer.Write(nAngle);
    }
}

