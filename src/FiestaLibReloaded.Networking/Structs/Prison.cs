// <auto-generated />
namespace FiestaLibReloaded.Networking.Structs;

/// <summary>sizeof = 22</summary>
public class PROTO_NC_PRISON_ADD_GM_ACK
{
    public Name5 sImprison = new();
    public ushort err;

    public void Read(BinaryReader reader)
    {
        sImprison.Read(reader);
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sImprison.Write(writer);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 102</summary>
public class PROTO_NC_PRISON_ADD_GM_REQ
{
    public Name5 sImprison = new();
    public ushort nMinute;
    public sbyte[] sReason = new sbyte[16];
    public sbyte[] sRemark = new sbyte[64];

    public void Read(BinaryReader reader)
    {
        sImprison.Read(reader);
        nMinute = reader.ReadUInt16();
        for (int i = 0; i < 16; i++)
            sReason[i] = reader.ReadSByte();
        for (int i = 0; i < 64; i++)
            sRemark[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        sImprison.Write(writer);
        writer.Write(nMinute);
        for (int i = 0; i < 16; i++)
            writer.Write(sReason[i]);
        for (int i = 0; i < 64; i++)
            writer.Write(sRemark[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 22</summary>
public class PROTO_NC_PRISON_ALTER_GM_ACK
{
    public Name5 sImprison = new();
    public ushort err;

    public void Read(BinaryReader reader)
    {
        sImprison.Read(reader);
        err = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sImprison.Write(writer);
        writer.Write(err);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 102</summary>
public class PROTO_NC_PRISON_ALTER_GM_REQ
{
    public Name5 sImprison = new();
    public ushort nMin;
    public sbyte[] sReason = new sbyte[16];
    public sbyte[] sRemark = new sbyte[64];

    public void Read(BinaryReader reader)
    {
        sImprison.Read(reader);
        nMin = reader.ReadUInt16();
        for (int i = 0; i < 16; i++)
            sReason[i] = reader.ReadSByte();
        for (int i = 0; i < 64; i++)
            sRemark[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        sImprison.Write(writer);
        writer.Write(nMin);
        for (int i = 0; i < 16; i++)
            writer.Write(sReason[i]);
        for (int i = 0; i < 64; i++)
            writer.Write(sRemark[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 6</summary>
public class PROTO_NC_PRISON_END_ACK
{
    public ushort err;
    public uint nPrisonSecond;

    public void Read(BinaryReader reader)
    {
        err = reader.ReadUInt16();
        nPrisonSecond = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(err);
        writer.Write(nPrisonSecond);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 1</summary>
public class PROTO_NC_PRISON_END_REQ
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 84</summary>
public class PROTO_NC_PRISON_GET_ACK
{
    public ushort err;
    public ushort nMinute;
    public sbyte[] sReason = new sbyte[16];
    public sbyte[] sRemark = new sbyte[64];

    public void Read(BinaryReader reader)
    {
        err = reader.ReadUInt16();
        nMinute = reader.ReadUInt16();
        for (int i = 0; i < 16; i++)
            sReason[i] = reader.ReadSByte();
        for (int i = 0; i < 64; i++)
            sRemark[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(err);
        writer.Write(nMinute);
        for (int i = 0; i < 16; i++)
            writer.Write(sReason[i]);
        for (int i = 0; i < 64; i++)
            writer.Write(sRemark[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

/// <summary>sizeof = 82</summary>
public class PROTO_NC_PRISON_OK_CMD
{
    public ushort nMinute;
    public sbyte[] sReason = new sbyte[16];
    public sbyte[] sRemark = new sbyte[64];

    public void Read(BinaryReader reader)
    {
        nMinute = reader.ReadUInt16();
        for (int i = 0; i < 16; i++)
            sReason[i] = reader.ReadSByte();
        for (int i = 0; i < 64; i++)
            sRemark[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nMinute);
        for (int i = 0; i < 16; i++)
            writer.Write(sReason[i]);
        for (int i = 0; i < 64; i++)
            writer.Write(sRemark[i]);
    }

    public void Read(Stream stream) => Read(new BinaryReader(stream, System.Text.Encoding.UTF8, true));
    public void Write(Stream stream) => Write(new BinaryWriter(stream, System.Text.Encoding.UTF8, true));
}

