// <auto-generated />
using FiestaLibReloaded.Networking;
using FiestaLibReloaded.Networking.Enums;

namespace FiestaLibReloaded.Networking.Structs;

/// <summary>
/// Department: User (0x03) | Opcode: 31 | Full: 0x031F
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 31)]
public class PROTO_NC_USER_AVATAR_LIST_REQ : IFiestaPacketBody
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 19 | Full: 0x0313
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 19)]
public class PROTO_NC_USER_AVATARINFO_ACK : IFiestaPacketBody
{
    public byte numofavatar;
    public PROTO_AVATARINFORMATION[] avatar = [];

    public void Read(BinaryReader reader)
    {
        numofavatar = reader.ReadByte();
        avatar = new PROTO_AVATARINFORMATION[numofavatar];
        for (int i = 0; i < numofavatar; i++)
        {
            avatar[i] = new PROTO_AVATARINFORMATION();
            avatar[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(numofavatar);
        for (int i = 0; i < avatar.Length; i++)
            avatar[i].Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 18 | Full: 0x0312
/// sizeof = 6
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 18)]
public class PROTO_NC_USER_AVATARINFO_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public uint usrregnum;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        usrregnum = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(usrregnum);
    }
}

/// <summary>sizeof = 9</summary>
public class PROTO_NC_USER_AVATARINFO_REQ_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_AVATARINFO_REQ req = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        req.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        req.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 84 | Full: 0x0354
/// sizeof = 287
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 84)]
public class PROTO_NC_USER_CH_PASSWORD_CHECK_ACK : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public Name256Byte id = new();
    public Name4 password = new();
    public uint usrregnum;
    public byte authid;
    public uint block;
    public uint loginable;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        id.Read(reader);
        password.Read(reader);
        usrregnum = reader.ReadUInt32();
        authid = reader.ReadByte();
        block = reader.ReadUInt32();
        loginable = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        id.Write(writer);
        password.Write(writer);
        writer.Write(usrregnum);
        writer.Write(authid);
        writer.Write(block);
        writer.Write(loginable);
    }
}

/// <summary>sizeof = 290</summary>
public class PROTO_NC_USER_CH_PASSWORD_CHECK_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_CH_PASSWORD_CHECK_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 83 | Full: 0x0353
/// sizeof = 274
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 83)]
public class PROTO_NC_USER_CH_PASSWORD_CHECK_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public Name256Byte user = new();
    public Name4 password = new();

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        user.Read(reader);
        password.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        user.Write(writer);
        password.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 103 | Full: 0x0367
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 103)]
public class PROTO_NC_USER_CLIENT_RIGHTVERSION_CHECK_ACK : IFiestaPacketBody
{
    public byte XTrapServerKeyLength;
    public byte[] XTrapServerKey = [];

    public void Read(BinaryReader reader)
    {
        XTrapServerKeyLength = reader.ReadByte();
        XTrapServerKey = reader.ReadBytes(XTrapServerKeyLength);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(XTrapServerKeyLength);
        writer.Write(XTrapServerKey);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 101 | Full: 0x0365
/// sizeof = 64
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 101)]
public class PROTO_NC_USER_CLIENT_VERSION_CHECK_REQ : IFiestaPacketBody
{
    public sbyte[] sVersionKey = new sbyte[64];

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 64; i++)
            sVersionKey[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 64; i++)
            writer.Write(sVersionKey[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 102 | Full: 0x0366
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 102)]
public class PROTO_NC_USER_CLIENT_WRONGVERSION_CHECK_ACK : IFiestaPacketBody
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 23 | Full: 0x0317
/// sizeof = 2
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 23)]
public class PROTO_NC_USER_CONNECTCUT_CMD : IFiestaPacketBody
{
    public ushort reason;

    public void Read(BinaryReader reader)
    {
        reason = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(reason);
    }
}

/// <summary>sizeof = 5</summary>
public class PROTO_NC_USER_CONNECTCUT_CMD_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_CONNECTCUT_CMD cmd = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        cmd.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        cmd.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 26 | Full: 0x031A
/// sizeof = 8
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 26)]
public class PROTO_NC_USER_CONNECTCUT2WORLDMANAGER_CMD : IFiestaPacketBody
{
    public uint usrregnum;
    public ushort wldmanhandle;
    public ushort reason;

    public void Read(BinaryReader reader)
    {
        usrregnum = reader.ReadUInt32();
        wldmanhandle = reader.ReadUInt16();
        reason = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(usrregnum);
        writer.Write(wldmanhandle);
        writer.Write(reason);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 25 | Full: 0x0319
/// sizeof = 4
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 25)]
public class PROTO_NC_USER_CONNECTCUT2ZONE_CMD : IFiestaPacketBody
{
    public uint usrregnum;

    public void Read(BinaryReader reader)
    {
        usrregnum = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(usrregnum);
    }
}

/// <summary>sizeof = 13</summary>
public class PROTO_NC_USER_CONNECTCUT2ZONE_CMD_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public NETPACKETZONEHEADER netpacketzoneheader = new();
    public PROTO_NC_USER_CONNECTCUT2ZONE_CMD cmd = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        netpacketzoneheader.Read(reader);
        cmd.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        netpacketzoneheader.Write(writer);
        cmd.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 54 | Full: 0x0336
/// sizeof = 36
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 54)]
public class PROTO_NC_USER_CREATE_OTP_ACK : IFiestaPacketBody
{
    public ushort nError;
    public ushort hWM;
    public Name8 sOTP = new();

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        hWM = reader.ReadUInt16();
        sOTP.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        writer.Write(hWM);
        sOTP.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 53 | Full: 0x0335
/// sizeof = 266
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 53)]
public class PROTO_NC_USER_CREATE_OTP_REQ : IFiestaPacketBody
{
    public ushort hWM;
    public uint nUserNo;
    public Name256Byte sUserID = new();
    public in_addr nClientIP = new();

    public void Read(BinaryReader reader)
    {
        hWM = reader.ReadUInt16();
        nUserNo = reader.ReadUInt32();
        sUserID.Read(reader);
        nClientIP.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(hWM);
        writer.Write(nUserNo);
        sUserID.Write(writer);
        nClientIP.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 60 | Full: 0x033C
/// sizeof = 290
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 60)]
public class PROTO_NC_USER_GER_IS_IP_BLOCK_ACK : IFiestaPacketBody
{
    public PROTO_NC_USER_GER_PASSWORD_CHECK_ACK LoginData = new();
    public ushort nClientHandle;
    public byte bUserIPBlock;

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nClientHandle = reader.ReadUInt16();
        bUserIPBlock = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nClientHandle);
        writer.Write(bUserIPBlock);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 59 | Full: 0x033B
/// sizeof = 313
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 59)]
public class PROTO_NC_USER_GER_IS_IP_BLOCK_REQ : IFiestaPacketBody
{
    public PROTO_NC_USER_GER_PASSWORD_CHECK_ACK LoginData = new();
    public uint nUserNo;
    public ushort nClientHandle;
    public sbyte[] sUserIP = new sbyte[20];

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nUserNo = reader.ReadUInt32();
        nClientHandle = reader.ReadUInt16();
        for (int i = 0; i < 20; i++)
            sUserIP[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nUserNo);
        writer.Write(nClientHandle);
        for (int i = 0; i < 20; i++)
            writer.Write(sUserIP[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 58 | Full: 0x033A
/// sizeof = 287
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 58)]
public class PROTO_NC_USER_GER_PASSWORD_CHECK_ACK : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public Name256Byte id = new();
    public Name4 password = new();
    public uint usrregnum;
    public byte authid;
    public uint block;
    public uint loginable;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        id.Read(reader);
        password.Read(reader);
        usrregnum = reader.ReadUInt32();
        authid = reader.ReadByte();
        block = reader.ReadUInt32();
        loginable = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        id.Write(writer);
        password.Write(writer);
        writer.Write(usrregnum);
        writer.Write(authid);
        writer.Write(block);
        writer.Write(loginable);
    }
}

/// <summary>sizeof = 290</summary>
public class PROTO_NC_USER_GER_PASSWORD_CHECK_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_GER_PASSWORD_CHECK_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 57 | Full: 0x0339
/// sizeof = 274
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 57)]
public class PROTO_NC_USER_GER_PASSWORD_CHECK_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public Name256Byte user = new();
    public Name4 password = new();

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        user.Read(reader);
        password.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        user.Write(writer);
        password.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 43 | Full: 0x032B
/// sizeof = 275
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 43)]
public class PROTO_NC_USER_IS_IP_BLOCK_ACK : IFiestaPacketBody
{
    public PROTO_NC_USER_LOGIN_REQ LoginData = new();
    public ushort nClientHandle;
    public byte bUserIPBlock;

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nClientHandle = reader.ReadUInt16();
        bUserIPBlock = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nClientHandle);
        writer.Write(bUserIPBlock);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 42 | Full: 0x032A
/// sizeof = 298
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 42)]
public class PROTO_NC_USER_IS_IP_BLOCK_REQ : IFiestaPacketBody
{
    public PROTO_NC_USER_LOGIN_REQ LoginData = new();
    public uint nUserNo;
    public ushort nClientHandle;
    public sbyte[] sUserIP = new sbyte[20];

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nUserNo = reader.ReadUInt32();
        nClientHandle = reader.ReadUInt16();
        for (int i = 0; i < 20; i++)
            sUserIP[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nUserNo);
        writer.Write(nClientHandle);
        for (int i = 0; i < 20; i++)
            writer.Write(sUserIP[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 78 | Full: 0x034E
/// sizeof = 309
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 78)]
public class PROTO_NC_USER_JP_IS_IP_BLOCK_ACK : IFiestaPacketBody
{
    public PROTO_NC_USER_JP_PASSWORD_CHECK_ACK LoginData = new();
    public ushort nClientHandle;
    public byte bUserIPBlock;

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nClientHandle = reader.ReadUInt16();
        bUserIPBlock = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nClientHandle);
        writer.Write(bUserIPBlock);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 77 | Full: 0x034D
/// sizeof = 332
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 77)]
public class PROTO_NC_USER_JP_IS_IP_BLOCK_REQ : IFiestaPacketBody
{
    public PROTO_NC_USER_JP_PASSWORD_CHECK_ACK LoginData = new();
    public uint nUserNo;
    public ushort nClientHandle;
    public sbyte[] sUserIP = new sbyte[20];

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nUserNo = reader.ReadUInt32();
        nClientHandle = reader.ReadUInt16();
        for (int i = 0; i < 20; i++)
            sUserIP[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nUserNo);
        writer.Write(nClientHandle);
        for (int i = 0; i < 20; i++)
            writer.Write(sUserIP[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 76 | Full: 0x034C
/// sizeof = 306
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 76)]
public class PROTO_NC_USER_JP_PASSWORD_CHECK_ACK : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public ushort nError;
    public uint nUserNo;
    public sbyte[] sUserName = new sbyte[24];
    public sbyte[] sChannel = new sbyte[254];
    public sbyte[] sUserIP = new sbyte[20];

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        nError = reader.ReadUInt16();
        nUserNo = reader.ReadUInt32();
        for (int i = 0; i < 24; i++)
            sUserName[i] = reader.ReadSByte();
        for (int i = 0; i < 254; i++)
            sChannel[i] = reader.ReadSByte();
        for (int i = 0; i < 20; i++)
            sUserIP[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(nError);
        writer.Write(nUserNo);
        for (int i = 0; i < 24; i++)
            writer.Write(sUserName[i]);
        for (int i = 0; i < 254; i++)
            writer.Write(sChannel[i]);
        for (int i = 0; i < 20; i++)
            writer.Write(sUserIP[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 75 | Full: 0x034B
/// sizeof = 336
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 75)]
public class PROTO_NC_USER_JP_PASSWORD_CHECK_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public sbyte[] sUserName = new sbyte[24];
    public sbyte[] sPassword = new sbyte[36];
    public sbyte[] sChannel = new sbyte[254];
    public sbyte[] sUserIP = new sbyte[20];

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        for (int i = 0; i < 24; i++)
            sUserName[i] = reader.ReadSByte();
        for (int i = 0; i < 36; i++)
            sPassword[i] = reader.ReadSByte();
        for (int i = 0; i < 254; i++)
            sChannel[i] = reader.ReadSByte();
        for (int i = 0; i < 20; i++)
            sUserIP[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        for (int i = 0; i < 24; i++)
            writer.Write(sUserName[i]);
        for (int i = 0; i < 36; i++)
            writer.Write(sPassword[i]);
        for (int i = 0; i < 254; i++)
            writer.Write(sChannel[i]);
        for (int i = 0; i < 20; i++)
            writer.Write(sUserIP[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 22 | Full: 0x0316
/// sizeof = 4
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 22)]
public class PROTO_NC_USER_KICKOFFFROMWORLD_CMD : IFiestaPacketBody
{
    public uint usrregnum;

    public void Read(BinaryReader reader)
    {
        usrregnum = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(usrregnum);
    }
}

/// <summary>sizeof = 13</summary>
public class PROTO_NC_USER_KICKOFFFROMWORLD_CMD_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public NETPACKETZONEHEADER netpacketzoneheader = new();
    public PROTO_NC_USER_KICKOFFFROMWORLD_CMD cmd = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        netpacketzoneheader.Read(reader);
        cmd.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        netpacketzoneheader.Write(writer);
        cmd.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 10 | Full: 0x030A
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 10)]
public class PROTO_NC_USER_LOGIN_ACK : IFiestaPacketBody
{
    public byte numofworld;
    // PROTO_NC_USER_LOGIN_ACK::WorldInfo[0] worldinfo at offset 1 (unsupported type)

    public void Read(BinaryReader reader)
    {
        numofworld = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(numofworld);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 16 | Full: 0x0310
/// sizeof = 9
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 16)]
public class PROTO_NC_USER_LOGIN_DB : IFiestaPacketBody
{
    public uint usrregnum;
    public byte worldnum;
    public byte[] ip = new byte[4];

    public void Read(BinaryReader reader)
    {
        usrregnum = reader.ReadUInt32();
        worldnum = reader.ReadByte();
        ip = reader.ReadBytes(4);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(usrregnum);
        writer.Write(worldnum);
        writer.Write(ip, 0, 4);
    }
}

/// <summary>sizeof = 12</summary>
public class PROTO_NC_USER_LOGIN_DB_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_LOGIN_DB db = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        db.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        db.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 30 | Full: 0x031E
/// sizeof = 278
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 30)]
public class PROTO_NC_USER_LOGIN_NETMARBLE_DB_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public byte[] UniID = new byte[20];
    public byte[] UserID = new byte[256];

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        UniID = reader.ReadBytes(20);
        UserID = reader.ReadBytes(256);
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(UniID, 0, 20);
        writer.Write(UserID, 0, 256);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 29 | Full: 0x031D
/// sizeof = 2
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 29)]
public class PROTO_NC_USER_LOGIN_NETMARBLE_REQ : IFiestaPacketBody
{
    public ushort CPCookieLength;
    public byte[] CPCookie = [];

    public void Read(BinaryReader reader)
    {
        CPCookieLength = reader.ReadUInt16();
        CPCookie = reader.ReadBytes(CPCookieLength);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(CPCookieLength);
        writer.Write(CPCookie);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 32 | Full: 0x0320
/// sizeof = 85
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 32)]
public class PROTO_NC_USER_LOGIN_OUTSPARK_REQ : IFiestaPacketBody
{
    public byte[] LogonToken = new byte[65];
    public Name5 spawnapps = new();

    public void Read(BinaryReader reader)
    {
        LogonToken = reader.ReadBytes(65);
        spawnapps.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(LogonToken, 0, 65);
        spawnapps.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 6 | Full: 0x0306
/// sizeof = 272
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 6)]
public class PROTO_NC_USER_LOGIN_REQ : IFiestaPacketBody
{
    public Name256Byte user = new();
    public Name4 password = new();

    public void Read(BinaryReader reader)
    {
        user.Read(reader);
        password.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        user.Write(writer);
        password.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 55 | Full: 0x0337
/// sizeof = 32
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 55)]
public class PROTO_NC_USER_LOGIN_WITH_OTP_REQ : IFiestaPacketBody
{
    public Name8 sOTP = new();

    public void Read(BinaryReader reader)
    {
        sOTP.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sOTP.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 9 | Full: 0x0309
/// sizeof = 2
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 9)]
public class PROTO_NC_USER_LOGINFAIL_ACK : IFiestaPacketBody
{
    public ushort error;

    public void Read(BinaryReader reader)
    {
        error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(error);
    }
}

/// <summary>sizeof = 5</summary>
public class PROTO_NC_USER_LOGINFAIL_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_LOGINFAIL_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 20 | Full: 0x0314
/// sizeof = 3
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 20)]
public class PROTO_NC_USER_LOGINWORLD_ACK : IFiestaPacketBody
{
    public ushort worldmanager;
    public byte numofavatar;
    public PROTO_AVATARINFORMATION[] avatar = [];

    public void Read(BinaryReader reader)
    {
        worldmanager = reader.ReadUInt16();
        numofavatar = reader.ReadByte();
        avatar = new PROTO_AVATARINFORMATION[numofavatar];
        for (int i = 0; i < numofavatar; i++)
        {
            avatar[i] = new PROTO_AVATARINFORMATION();
            avatar[i].Read(reader);
        }
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(worldmanager);
        writer.Write(numofavatar);
        for (int i = 0; i < avatar.Length; i++)
            avatar[i].Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 15 | Full: 0x030F
/// sizeof = 320
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 15)]
public class PROTO_NC_USER_LOGINWORLD_REQ : IFiestaPacketBody
{
    public Name256Byte user = new();
    public ushort[] validate_new = new ushort[32];

    public void Read(BinaryReader reader)
    {
        user.Read(reader);
        for (int i = 0; i < 32; i++)
            validate_new[i] = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        user.Write(writer);
        for (int i = 0; i < 32; i++)
            writer.Write(validate_new[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 21 | Full: 0x0315
/// sizeof = 2
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 21)]
public class PROTO_NC_USER_LOGINWORLDFAIL_ACK : IFiestaPacketBody
{
    public PROTO_ERRORCODE errorcode = new();

    public void Read(BinaryReader reader)
    {
        errorcode.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        errorcode.Write(writer);
    }
}

/// <summary>sizeof = 5</summary>
public class PROTO_NC_USER_LOGINWORLDFAIL_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_LOGINWORLDFAIL_ACK Ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        Ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        Ack.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 17 | Full: 0x0311
/// sizeof = 9
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 17)]
public class PROTO_NC_USER_LOGOUT_DB : IFiestaPacketBody
{
    public uint usrregnum;
    public byte worldnum;
    public ushort playmin;
    public ushort error;

    public void Read(BinaryReader reader)
    {
        usrregnum = reader.ReadUInt32();
        worldnum = reader.ReadByte();
        playmin = reader.ReadUInt16();
        error = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(usrregnum);
        writer.Write(worldnum);
        writer.Write(playmin);
        writer.Write(error);
    }
}

/// <summary>sizeof = 12</summary>
public class PROTO_NC_USER_LOGOUT_DB_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_LOGOUT_DB db = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        db.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        db.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 99 | Full: 0x0363
/// sizeof = 10
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 99)]
public class PROTO_NC_USER_LOGOUT_LAST_DAY_ACK : IFiestaPacketBody
{
    public ushort nHandle;
    public uint nUserNo;
    public int tLastSec;

    public void Read(BinaryReader reader)
    {
        nHandle = reader.ReadUInt16();
        nUserNo = reader.ReadUInt32();
        tLastSec = reader.ReadInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nHandle);
        writer.Write(nUserNo);
        writer.Write(tLastSec);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 98 | Full: 0x0362
/// sizeof = 6
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 98)]
public class PROTO_NC_USER_LOGOUT_LAST_DAY_REQ : IFiestaPacketBody
{
    public ushort nHandle;
    public uint nUserNo;

    public void Read(BinaryReader reader)
    {
        nHandle = reader.ReadUInt16();
        nUserNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nHandle);
        writer.Write(nUserNo);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 24 | Full: 0x0318
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 24)]
public class PROTO_NC_USER_NORMALLOGOUT_CMD : IFiestaPacketBody
{
    public byte LogoutType;

    public void Read(BinaryReader reader)
    {
        LogoutType = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(LogoutType);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 8 | Full: 0x0308
/// sizeof = 287
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 8)]
public class PROTO_NC_USER_PASSWORD_CHECK_ACK : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public Name256Byte id = new();
    public Name4 password = new();
    public uint usrregnum;
    public byte authid;
    public uint block;
    public uint loginable;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        id.Read(reader);
        password.Read(reader);
        usrregnum = reader.ReadUInt32();
        authid = reader.ReadByte();
        block = reader.ReadUInt32();
        loginable = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        id.Write(writer);
        password.Write(writer);
        writer.Write(usrregnum);
        writer.Write(authid);
        writer.Write(block);
        writer.Write(loginable);
    }
}

/// <summary>sizeof = 290</summary>
public class PROTO_NC_USER_PASSWORD_CHECK_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_PASSWORD_CHECK_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 7 | Full: 0x0307
/// sizeof = 274
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 7)]
public class PROTO_NC_USER_PASSWORD_CHECK_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public Name256Byte user = new();
    public Name4 password = new();

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        user.Read(reader);
        password.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        user.Write(writer);
        password.Write(writer);
    }
}

/// <summary>sizeof = 277</summary>
public class PROTO_NC_USER_PASSWORD_CHECK_REQ_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_PASSWORD_CHECK_REQ req = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        req.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        req.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 49 | Full: 0x0331
/// sizeof = 6
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 49)]
public class PROTO_NC_USER_POSSIBLE_NEW_CONNECT_CMD : IFiestaPacketBody
{
    public ushort nWorldManagerHandle;
    public uint nCharNo;

    public void Read(BinaryReader reader)
    {
        nWorldManagerHandle = reader.ReadUInt16();
        nCharNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nWorldManagerHandle);
        writer.Write(nCharNo);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 36 | Full: 0x0324
/// sizeof = 4
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 36)]
public class PROTO_NC_USER_REGISENUMBER_ACK : IFiestaPacketBody
{
    public uint userregnum;

    public void Read(BinaryReader reader)
    {
        userregnum = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(userregnum);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 97 | Full: 0x0361
/// sizeof = 11
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 97)]
public class PROTO_NC_USER_RETURN_CHECK_ACK : IFiestaPacketBody
{
    public ushort nHandle;
    public uint nUserNo;
    public byte bReturnUser;
    public int tLastSec;

    public void Read(BinaryReader reader)
    {
        nHandle = reader.ReadUInt16();
        nUserNo = reader.ReadUInt32();
        bReturnUser = reader.ReadByte();
        tLastSec = reader.ReadInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nHandle);
        writer.Write(nUserNo);
        writer.Write(bReturnUser);
        writer.Write(tLastSec);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 96 | Full: 0x0360
/// sizeof = 6
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 96)]
public class PROTO_NC_USER_RETURN_CHECK_REQ : IFiestaPacketBody
{
    public ushort nHandle;
    public uint nUserNo;

    public void Read(BinaryReader reader)
    {
        nHandle = reader.ReadUInt16();
        nUserNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nHandle);
        writer.Write(nUserNo);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 100 | Full: 0x0364
/// sizeof = 4
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 100)]
public class PROTO_NC_USER_SET_RETURN_CMD : IFiestaPacketBody
{
    public uint nUserNo;

    public void Read(BinaryReader reader)
    {
        nUserNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nUserNo);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 39 | Full: 0x0327
/// sizeof = 313
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 39)]
public class PROTO_NC_USER_TEENAGER_ACK : IFiestaPacketBody
{
    public Name256Byte sUserID = new();
    public ushort nHandle;
    public PROTO_NC_USER_TEENAGER_CMD Data = new();

    public void Read(BinaryReader reader)
    {
        sUserID.Read(reader);
        nHandle = reader.ReadUInt16();
        Data.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        sUserID.Write(writer);
        writer.Write(nHandle);
        Data.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 37 | Full: 0x0325
/// sizeof = 55
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 37)]
public class PROTO_NC_USER_TEENAGER_CMD : IFiestaPacketBody
{
    public byte[] sUserKey = new byte[50];
    public byte nType;
    public uint nMinute;

    public void Read(BinaryReader reader)
    {
        sUserKey = reader.ReadBytes(50);
        nType = reader.ReadByte();
        nMinute = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(sUserKey, 0, 50);
        writer.Write(nType);
        writer.Write(nMinute);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 38 | Full: 0x0326
/// sizeof = 258
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 38)]
public class PROTO_NC_USER_TEENAGER_REQ : IFiestaPacketBody
{
    public Name256Byte sUserID = new();
    public ushort nHandle;

    public void Read(BinaryReader reader)
    {
        sUserID.Read(reader);
        nHandle = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        sUserID.Write(writer);
        writer.Write(nHandle);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 40 | Full: 0x0328
/// sizeof = 54
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 40)]
public class PROTO_NC_USER_TEENAGER_SET_CMD : IFiestaPacketBody
{
    public byte[] sUserKey = new byte[50];
    public uint nUserPlayRemainTime;

    public void Read(BinaryReader reader)
    {
        sUserKey = reader.ReadBytes(50);
        nUserPlayRemainTime = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(sUserKey, 0, 50);
        writer.Write(nUserPlayRemainTime);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 95 | Full: 0x035F
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 95)]
public class PROTO_NC_USER_TUTORIAL_CAN_SKIP_CMD : IFiestaPacketBody
{
    public byte bCanSkip;

    public void Read(BinaryReader reader)
    {
        bCanSkip = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bCanSkip);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 65 | Full: 0x0341
/// sizeof = 11
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 65)]
public class PROTO_NC_USER_TW_IS_IP_BLOCK_ACK : IFiestaPacketBody
{
    public PROTO_NC_USER_TW_PASSWORD_CHECK_ACK LoginData = new();
    public ushort nClientHandle;
    public byte bUserIPBlock;

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nClientHandle = reader.ReadUInt16();
        bUserIPBlock = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nClientHandle);
        writer.Write(bUserIPBlock);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 64 | Full: 0x0340
/// sizeof = 34
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 64)]
public class PROTO_NC_USER_TW_IS_IP_BLOCK_REQ : IFiestaPacketBody
{
    public PROTO_NC_USER_TW_PASSWORD_CHECK_ACK LoginData = new();
    public uint nUserNo;
    public ushort nClientHandle;
    public sbyte[] sUserIP = new sbyte[20];

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nUserNo = reader.ReadUInt32();
        nClientHandle = reader.ReadUInt16();
        for (int i = 0; i < 20; i++)
            sUserIP[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nUserNo);
        writer.Write(nClientHandle);
        for (int i = 0; i < 20; i++)
            writer.Write(sUserIP[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 63 | Full: 0x033F
/// sizeof = 8
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 63)]
public class PROTO_NC_USER_TW_PASSWORD_CHECK_ACK : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public ushort nError;
    public uint nUserNo;

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        nError = reader.ReadUInt16();
        nUserNo = reader.ReadUInt32();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(nError);
        writer.Write(nUserNo);
    }
}

/// <summary>sizeof = 11</summary>
public class PROTO_NC_USER_TW_PASSWORD_CHECK_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_TW_PASSWORD_CHECK_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 62 | Full: 0x033E
/// sizeof = 102
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 62)]
public class PROTO_NC_USER_TW_PASSWORD_CHECK_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public sbyte[] sUserName = new sbyte[36];
    public sbyte[] sSerial = new sbyte[36];
    public sbyte[] sUID = new sbyte[20];
    public sbyte[] sSID = new sbyte[8];

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        for (int i = 0; i < 36; i++)
            sUserName[i] = reader.ReadSByte();
        for (int i = 0; i < 36; i++)
            sSerial[i] = reader.ReadSByte();
        for (int i = 0; i < 20; i++)
            sUID[i] = reader.ReadSByte();
        for (int i = 0; i < 8; i++)
            sSID[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        for (int i = 0; i < 36; i++)
            writer.Write(sUserName[i]);
        for (int i = 0; i < 36; i++)
            writer.Write(sSerial[i]);
        for (int i = 0; i < 20; i++)
            writer.Write(sUID[i]);
        for (int i = 0; i < 8; i++)
            writer.Write(sSID[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 94 | Full: 0x035E
/// sizeof = 356
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 94)]
public class PROTO_NC_USER_US_IS_IP_BLOCK_ACK : IFiestaPacketBody
{
    public PROTO_NC_USER_US_PASSWORD_CHECK_ACK LoginData = new();
    public ushort nClientHandle;
    public byte bUserIPBlock;

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nClientHandle = reader.ReadUInt16();
        bUserIPBlock = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nClientHandle);
        writer.Write(bUserIPBlock);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 93 | Full: 0x035D
/// sizeof = 379
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 93)]
public class PROTO_NC_USER_US_IS_IP_BLOCK_REQ : IFiestaPacketBody
{
    public PROTO_NC_USER_US_PASSWORD_CHECK_ACK LoginData = new();
    public uint nUserNo;
    public ushort nClientHandle;
    public sbyte[] sUserIP = new sbyte[20];

    public void Read(BinaryReader reader)
    {
        LoginData.Read(reader);
        nUserNo = reader.ReadUInt32();
        nClientHandle = reader.ReadUInt16();
        for (int i = 0; i < 20; i++)
            sUserIP[i] = reader.ReadSByte();
    }

    public void Write(BinaryWriter writer)
    {
        LoginData.Write(writer);
        writer.Write(nUserNo);
        writer.Write(nClientHandle);
        for (int i = 0; i < 20; i++)
            writer.Write(sUserIP[i]);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 90 | Full: 0x035A
/// sizeof = 316
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 90)]
public class PROTO_NC_USER_US_LOGIN_REQ : IFiestaPacketBody
{
    public sbyte[] sUserName = new sbyte[260];
    public sbyte[] sPassword = new sbyte[36];
    public Name5 spawnapps = new();

    public void Read(BinaryReader reader)
    {
        for (int i = 0; i < 260; i++)
            sUserName[i] = reader.ReadSByte();
        for (int i = 0; i < 36; i++)
            sPassword[i] = reader.ReadSByte();
        spawnapps.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        for (int i = 0; i < 260; i++)
            writer.Write(sUserName[i]);
        for (int i = 0; i < 36; i++)
            writer.Write(sPassword[i]);
        spawnapps.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 92 | Full: 0x035C
/// sizeof = 353
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 92)]
public class PROTO_NC_USER_US_PASSWORD_CHECK_ACK : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public ushort nError;
    public uint nUserNo;
    public sbyte[] sUserName = new sbyte[260];
    public sbyte[] sPassword = new sbyte[36];
    public sbyte[] sUserIP = new sbyte[20];
    public byte nAuthID;
    public uint nBlock;
    public uint bLoginAble;
    public Name5 spawnapps = new();

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        nError = reader.ReadUInt16();
        nUserNo = reader.ReadUInt32();
        for (int i = 0; i < 260; i++)
            sUserName[i] = reader.ReadSByte();
        for (int i = 0; i < 36; i++)
            sPassword[i] = reader.ReadSByte();
        for (int i = 0; i < 20; i++)
            sUserIP[i] = reader.ReadSByte();
        nAuthID = reader.ReadByte();
        nBlock = reader.ReadUInt32();
        bLoginAble = reader.ReadUInt32();
        spawnapps.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(nError);
        writer.Write(nUserNo);
        for (int i = 0; i < 260; i++)
            writer.Write(sUserName[i]);
        for (int i = 0; i < 36; i++)
            writer.Write(sPassword[i]);
        for (int i = 0; i < 20; i++)
            writer.Write(sUserIP[i]);
        writer.Write(nAuthID);
        writer.Write(nBlock);
        writer.Write(bLoginAble);
        spawnapps.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 91 | Full: 0x035B
/// sizeof = 338
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 91)]
public class PROTO_NC_USER_US_PASSWORD_CHECK_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public sbyte[] sUserName = new sbyte[260];
    public sbyte[] sPassword = new sbyte[36];
    public sbyte[] sUserIP = new sbyte[20];
    public Name5 spawnapps = new();

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        for (int i = 0; i < 260; i++)
            sUserName[i] = reader.ReadSByte();
        for (int i = 0; i < 36; i++)
            sPassword[i] = reader.ReadSByte();
        for (int i = 0; i < 20; i++)
            sUserIP[i] = reader.ReadSByte();
        spawnapps.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        for (int i = 0; i < 260; i++)
            writer.Write(sUserName[i]);
        for (int i = 0; i < 36; i++)
            writer.Write(sPassword[i]);
        for (int i = 0; i < 20; i++)
            writer.Write(sUserIP[i]);
        spawnapps.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 50 | Full: 0x0332
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 50)]
public class PROTO_NC_USER_USE_BEAUTY_SHOP_CMD : IFiestaPacketBody
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 52 | Full: 0x0334
/// sizeof = 34
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 52)]
public class PROTO_NC_USER_WILL_WORLD_SELECT_ACK : IFiestaPacketBody
{
    public ushort nError;
    public Name8 sOTP = new();

    public void Read(BinaryReader reader)
    {
        nError = reader.ReadUInt16();
        sOTP.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(nError);
        sOTP.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 51 | Full: 0x0333
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 51)]
public class PROTO_NC_USER_WILL_WORLD_SELECT_REQ : IFiestaPacketBody
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 14 | Full: 0x030E
/// sizeof = 348
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 14)]
public class PROTO_NC_USER_WILLLOGIN_ACK : IFiestaPacketBody
{
    public PROTO_NC_USER_WILLLOGIN_REQ will_login_req = new();
    public byte ok;

    public void Read(BinaryReader reader)
    {
        will_login_req.Read(reader);
        ok = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        will_login_req.Write(writer);
        writer.Write(ok);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 13 | Full: 0x030D
/// sizeof = 347
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 13)]
public class PROTO_NC_USER_WILLLOGIN_REQ : IFiestaPacketBody
{
    public NETPACKETHEADER netpacketheader = new();
    public byte willlogin_worldno;
    public uint usrregnum;
    public Name256Byte userid = new();
    public ushort[] validate_new = new ushort[32];
    public Name5 spawnapps = new();

    public void Read(BinaryReader reader)
    {
        netpacketheader.Read(reader);
        willlogin_worldno = reader.ReadByte();
        usrregnum = reader.ReadUInt32();
        userid.Read(reader);
        for (int i = 0; i < 32; i++)
            validate_new[i] = reader.ReadUInt16();
        spawnapps.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        netpacketheader.Write(writer);
        writer.Write(willlogin_worldno);
        writer.Write(usrregnum);
        userid.Write(writer);
        for (int i = 0; i < 32; i++)
            writer.Write(validate_new[i]);
        spawnapps.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 27 | Full: 0x031B
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 27)]
public class PROTO_NC_USER_WORLD_STATUS_REQ : IFiestaPacketBody
{

    public void Read(BinaryReader reader)
    {
    }

    public void Write(BinaryWriter writer)
    {
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 12 | Full: 0x030C
/// sizeof = 83
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 12)]
public class PROTO_NC_USER_WORLDSELECT_ACK : IFiestaPacketBody
{
    public byte worldstatus;
    public Name4 ip = new();
    public ushort port;
    public ushort[] validate_new = new ushort[32];

    public void Read(BinaryReader reader)
    {
        worldstatus = reader.ReadByte();
        ip.Read(reader);
        port = reader.ReadUInt16();
        for (int i = 0; i < 32; i++)
            validate_new[i] = reader.ReadUInt16();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(worldstatus);
        ip.Write(writer);
        writer.Write(port);
        for (int i = 0; i < 32; i++)
            writer.Write(validate_new[i]);
    }
}

/// <summary>sizeof = 86</summary>
public class PROTO_NC_USER_WORLDSELECT_ACK_SEND : IFiestaPacketBody
{
    public byte size;
    public NETCOMMAND netcmd = new();
    public PROTO_NC_USER_WORLDSELECT_ACK ack = new();

    public void Read(BinaryReader reader)
    {
        size = reader.ReadByte();
        netcmd.Read(reader);
        ack.Read(reader);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(size);
        netcmd.Write(writer);
        ack.Write(writer);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 11 | Full: 0x030B
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 11)]
public class PROTO_NC_USER_WORLDSELECT_REQ : IFiestaPacketBody
{
    public byte worldno;

    public void Read(BinaryReader reader)
    {
        worldno = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(worldno);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 5 | Full: 0x0305
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 5)]
public class PROTO_NC_USER_XTRAP_ACK : IFiestaPacketBody
{
    public byte bSuccess;

    public void Read(BinaryReader reader)
    {
        bSuccess = reader.ReadByte();
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(bSuccess);
    }
}

/// <summary>
/// Department: User (0x03) | Opcode: 4 | Full: 0x0304
/// sizeof = 1
/// </summary>
[FiestaOpcode(ProtocolCommand.User, 4)]
public class PROTO_NC_USER_XTRAP_REQ : IFiestaPacketBody
{
    public byte XTrapClientKeyLength;
    public byte[] XTrapClientKey = [];

    public void Read(BinaryReader reader)
    {
        XTrapClientKeyLength = reader.ReadByte();
        XTrapClientKey = reader.ReadBytes(XTrapClientKeyLength);
    }

    public void Write(BinaryWriter writer)
    {
        writer.Write(XTrapClientKeyLength);
        writer.Write(XTrapClientKey);
    }
}

